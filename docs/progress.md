# MultiChrome 进度快照

## 当前阶段

V0.4 基础可用性已完成第一段：账号卡片支持颜色标识、标准/紧凑视图切换、多选和批量标签操作。用户确认“备注就够用”，账号状态管理入口已从 UI 移除，底层 `status` 字段仅保留用于旧数据兼容。账号卡片已移除目录大小显示，目录大小只在编辑单个账号时按需刷新；运行状态改为账号页轻量动态刷新。外部图标区分实验已暂停，不纳入当前 MVP；当前继续回到普通多 Chrome profile 管理，并已补齐目录健康检查、一键低风险修复、轻量备份/恢复、完整 profile 文件夹备份/恢复、批量打开网址队列、常用/最近网址、项目入口、网址库表格版和账号平台资料库第一版这类基础能力。浏览器会话编排器 V0 已开始落地：后端新增批量 session snapshot，前端运行状态刷新已改为读取会话快照；V0.2 已让窗口操作入口在执行前先刷新轻量 session，避免依赖旧运行状态；V0.3 已给 session snapshot 增加明确 `status` 字段；V0.4 已加入 `windowCount`，区分“未运行 0 个窗口”和“运行中但轻量模式未扫描窗口”的未知状态；V0.5 已加入前端 `starting` 临时状态，启动命令成功后先显示“启动中”，只有轻量 session snapshot 确认进程存在后才进入“运行中”；V0.6 已加入启动后短延迟确认刷新，减少“启动中”等到 5 秒轮询才变化的迟滞；V0.7 已加入会话快照竞态保护，旧请求晚返回不会覆盖新状态；V0.8 已让账号页轻量轮询失败时保留上一次会话状态，避免临时扫描失败造成状态闪烁；V0.9 已给 fresh `starting` 会话加入短暂宽限期，避免 Chrome 慢启动时确认快照扫早导致“启动中”闪回停止态；V1.0 已把前端会话编排从 `App.tsx` 抽为 `browserSessions` 纯模型和 `useBrowserSessions` hook；V2.1 已开始把启动动作变成结构化结果，成功才标记 `starting`，失败返回可显示消息；V2.2 已给批量/项目启动增加结构化汇总，失败提示会显示账号名和原因；V2.3 已加入最近启动事件，账号页“更多操作”可查看最近 6 条启动结果，底层最多保留 30 条；V2.4 已把最近启动事件保存到 `app-data/launch-events.json`，不写入 `profiles.json`；V2.5 已让启动事件读取容忍损坏 sidecar 或单条坏记录，日志异常不影响主流程；V2.6 已把批量/项目启动队列的结束摘要和文案收敛到纯模型函数；V2.7 已新增失败启动结果生成重试候选的纯函数，但暂不做运行时进度状态机，避免过早抽象；V2.8 已把最近一次批量打开失败接成“重试失败”入口；V2.9 已让新一轮批量打开开始时清空旧重试入口，避免旧失败按钮在运行中继续误导；V2.10 已让批量网址输入上下文变化时清空旧重试入口，避免“当前输入框”和“旧失败队列”同时存在；V3.1 已把账号页批量打开网址接入 operation，更多操作里能看到最近批量 operation 的成功、失败或已取消状态；V3.2 已把项目打开接入 `project-open` operation，项目完成、失败或中途停止后也会进入最近操作记录；V3.3 已把检查窗口、前置窗口、平铺窗口和同步布局接入 `window-action` operation；V3.4 已把更多操作里的 operation 展示拆成“当前操作”和“最近操作”，运行中/排队中单独展示，已完成/失败/取消才进入最近记录；V3.5 已加入同账号 active operation 冲突守卫，批量打开、项目打开、窗口操作、单账号打开和账号平台打开会避开正在执行中的同 profile，并给启动/窗口命令加入 120 秒超时兜底，避免底层调用卡死后长期锁住账号。

版本编号说明：V2.0 是“启动动作结构化”阶段的总起点，早期记录直接从 V2.1 写起属于编号疏漏；功能上 V2 已覆盖结构化启动、汇总、日志、持久化、容错和重试。后续浏览器会话编排器从 V3.0 开始严格记录。V3.0 已起步：新增 `BrowserOperation` 纯模型，定义 operation 的 `queued/running/succeeded/failed/cancelled` 生命周期，并复用现有 `BrowserLaunchQueueSummary` 判断批量启动队列最终状态。V3 的目标是先做“操作编排中心”：V3.1 接入批量打开网址，V3.2 接入项目打开，V3.3 接入窗口操作，V3.4 做轻量最近/当前操作面板，V3.5 处理同 profile active operation 冲突边界。V4 再进入“窗口编排/同步基础”：窗口注册表、布局纯模型、布局预设、同步控制器和交互同步预研。V3.5 已完成；当前已完成 App 巨石拆分阶段 1、阶段 2、阶段 3、阶段 4 和阶段 5，并完成第二轮阶段 1“主工作流清晰度”、阶段 2“资料管理空状态与行操作”、阶段 3“设置与数据安全稳定感”和阶段 4“操作记录和窗口提示轻量收口”。优化线已完成 PR #18 到 PR #35，其中 PR #24 抽出 Settings 数据安全状态，PR #25 抽出 Settings 根目录/Chrome/主题草稿状态，PR #26 收口导入/恢复文档竞态，PR #27 加固实体 ID 复用下的 stale 写入边界，PR #28 抽出 profiles document mutation hook，PR #29 抽出 profiles document store hook，PR #30 移除 loaded root/restore 中重复 document setters，PR #31 把 profile document persist facade 移入 store hook，PR #32 命名 loaded root/restore 后的 UI 清理流程，PR #33 把 data safety restore 的 document queue/root guard/replace facade 移入 store hook，PR #34 把 loaded root document replacement facade 移入 store hook，PR #35 把导入成功后的 document commit plan 抽成纯模型。第二轮阶段 4 只打磨更多操作里的当前/最近操作和最近启动记录展示，不新增功能、不改变 operation 生命周期、不改变启动事件生成/保存/读取逻辑、不碰 Rust 后端或窗口 API 流程。

优化 PR #18 已完成第一段树干瘦身：分支 `codex/api-normalization-domain-model`，PR `https://github.com/1572817043/mutiChrome/pull/18`，commit `0c72c4b`。本轮只把 `src/api.ts` 里的 profile document、settings、profiles、projects、urlLibrary 归一化和空文档构造外移到 `src/domain/profileDocumentModel.ts`，`api.ts` 保留 `DEFAULT_BROWSER_PATH` 与 `normalizeSettings` 兼容导出并继续负责 Tauri/browser preview API 包装；不改 UI、Rust、样式或运行时功能。新增 `src/domain/profileDocumentModel.test.ts` 覆盖空 document、legacy `favoriteUrls -> urlLibrary`、已有 `urlLibrary` 优先、profile `accountPlatforms/importSource` 和 project legacy URL/interval fallback。阶段验证通过：`rtk npm test -- src/domain/profileDocumentModel.test.ts src/api.test.ts` 为 6 passed，`rtk npm test` 为 262 passed，`rtk npm run build` 通过，`rtk git diff --check --cached` 通过。Spec review 和 code quality review 均已通过；畸形持久化数组继续保持旧实现错误语义，不在本优化 PR 改变兼容策略。

优化 PR #19 已完成第二段树干瘦身：分支 `codex/settings-dialog-prop-groups`，PR `https://github.com/1572817043/mutiChrome/pull/19`，commit `1af1400`。本轮只把 `SettingsDialogProps` 从 44 个平铺字段收敛为 `rootSettings`、`health`、`lightBackup`、`fullBackup`、`runtimeDiagnostics` 和 `onClose` 6 个顶层 props；`App.tsx` 单一调用点改为按职责组装状态和 handler，`SettingsDialog` 内部仍按原 UI、文案、禁用条件和点击行为执行；不改 CSS、Rust 或业务功能。`SettingsDialog.test.tsx` 的 helper 改为分组覆盖，并修正局部分组 override 的 TypeScript 类型表达。阶段验证通过：`rtk npm test -- src/settings/SettingsDialog.test.tsx` 为 13 passed，`rtk npm test -- src/App.test.tsx -t "设置|开发诊断|根目录|备份"` 为 30 passed，`rtk npm test` 为 262 passed，`rtk npm run build` 通过，`rtk git diff --check` 通过。Spec review 和 code quality review 均已通过。

优化 PR #20 已完成第三段树干瘦身：分支 `codex/bulk-action-bar-prop-groups`，PR `https://github.com/1572817043/mutiChrome/pull/20`，commit `d7aad28`。本轮只把 `BulkActionBarProps` 从 33 个平铺字段收敛为 `selection`、`urlQueue`、`tagging`、`windowActions` 和 `activity` 5 个顶层 props；`App.tsx` 单一调用点改为按职责组装批量选择、URL 队列、标签、窗口操作和活动记录状态/handler；不改 UI 文案、CSS、批量打开、重试、停止、更多操作、窗口动作或删除选中行为。`BulkActionBar.test.tsx` 的 helper 改为分组覆盖。阶段验证通过：`rtk npm test -- src/bulk-actions/BulkActionBar.test.tsx` 为 3 passed，`rtk npm test -- src/App.test.tsx -t "批量|重试|更多操作|窗口|删除选中"` 为 51 passed，`rtk npm test` 为 262 passed，`rtk npm run build` 通过，`rtk git diff --check` 通过。Spec review 和 code quality review 均已通过。

优化 PR #21 已完成第四段树干瘦身：分支 `codex/rust-macos-accessibility-module`，PR `https://github.com/1572817043/mutiChrome/pull/21`，commit `10a5ab6`。本轮只把 `src-tauri/src/lib.rs` 内嵌的 macOS Accessibility 实现机械搬到 `src-tauri/src/macos_accessibility.rs`，`lib.rs` 仅保留 `#[cfg(target_os = "macos")] mod macos_accessibility;` 和原有 macOS/non-macOS wrapper；窗口切换、窗口检查、窗口平铺调用点、错误文案、非 macOS fallback、Tauri command、CDP 和 Chrome 启动逻辑均不改。`lib.rs` 从 3466 行降到 3104 行，新模块 346 行。阶段验证通过：`rtk cargo check --manifest-path src-tauri/Cargo.toml --locked --all-targets` 通过，`rtk cargo test --manifest-path src-tauri/Cargo.toml` 为 84 passed，`rtk cargo fmt --manifest-path src-tauri/Cargo.toml -- --check` 通过，`rtk git diff --check` 通过。Spec review 和 code quality review 均已通过；本轮未做真实 macOS 辅助功能窗口操作手测，因为只是模块搬迁。

优化 PR #22 已完成第五段树干瘦身：分支 `codex/rust-cdp-runtime-module`，PR `https://github.com/1572817043/mutiChrome/pull/22`，commit `81a750c`。本轮只把 `src-tauri/src/lib.rs` 中 Browser Runtime / CDP 的类型、HTTP `/json/list`/`/json/version` 探测、WebSocket `Page.navigate`、URL 校验、tab 映射和相关 helper 机械搬到 `src-tauri/src/browser_runtime.rs`；`list_runtime_tabs`、`navigate_runtime_tab` 和 `snapshot_browser_sessions` 入口仍留在 `lib.rs` 并委托新模块，进程扫描、profile process 解析、session snapshot、窗口逻辑和 Chrome 启动 debug port 分配均不改。`lib.rs` 从 3104 行降到 2500 行，新模块 624 行。阶段验证通过：`rtk cargo check --manifest-path src-tauri/Cargo.toml --locked --all-targets` 通过，`rtk cargo test --manifest-path src-tauri/Cargo.toml` 为 84 passed，`rtk cargo fmt --manifest-path src-tauri/Cargo.toml -- --check` 通过，`rtk git diff --check` 通过。Spec review 和 code quality review 均已通过；现有 CDP 测试仍在 `lib.rs` 测试模块中通过 `browser_runtime` 访问内部 helper，后续可迁入新模块以收紧 `pub(crate)`。

优化 PR #23 已完成第六段树干瘦身：分支 `codex/app-settings-controller`，PR `https://github.com/1572817043/mutiChrome/pull/23`，commit `3b393fc`。本轮只把 `src/App.tsx` 中 Settings DEV“开发诊断”的状态、请求失效 refs、读取 Runtime 标签页、导航第一个 page 标签页和导航后轮询确认逻辑抽到 `src/settings/useRuntimeDiagnostics.ts`；App 仍负责单选账号和 session 映射，`SettingsDialog` UI、文案、props 契约、根目录、健康检查、备份恢复和业务流程均不改。`App.tsx` 从 3709 行降到 3457 行，新 hook 275 行。阶段验证通过：`rtk npm test -- src/App.test.tsx -t "开发诊断"` 为 10 passed，`rtk npm test -- src/settings/SettingsDialog.test.tsx` 为 13 passed，`rtk npm test` 为 262 passed，`rtk npm run build` 通过，`rtk git diff --check` 通过。Spec review 和 code quality review 均已通过；本轮未做真实 Tauri/CDP 桌面端集成测试，现有 App 测试已覆盖关闭 Settings、切换账号、切换 root 后旧请求不污染 UI。

优化 PR #24 已完成第七段树干瘦身：分支 `codex/app-data-safety-settings`，PR `https://github.com/1572817043/mutiChrome/pull/24`，commit `db094d7`。本轮只把 `src/App.tsx` 中 Settings 数据安全相关状态和 handler 抽到 `src/settings/useDataSafetySettings.ts`，包括目录健康检查、低风险修复、孤儿目录登记、轻量备份/恢复、完整备份预览/创建/恢复和确认弹窗状态；App 继续负责全局恢复落地和 profiles/settings/root 状态写入。`SettingsDialog` UI、文案、root reveal、CSS、Rust 后端和 `api.ts` 均不改。`App.tsx` 从 3457 行降到 3130 行，新 hook 415 行。新增/增强 App 测试锁定 Settings 关闭、备份恢复、完整恢复和切换根目录时的清理边界：关闭 Settings 只关闭确认框并保留备份路径；轻量恢复后保留轻量路径和完整备份路径；完整恢复后保留两类备份路径；切换根目录保留轻量备份路径但清空完整备份草稿，并保持进行中的健康检查 busy 状态。阶段验证通过：`rtk npm test -- src/App.test.tsx -t "切换根目录会保留轻量备份路径并清除完整备份草稿|设置弹窗关闭会清除备份恢复确认|健康检查|创建备份|完整备份|备份恢复|根目录"` 为 14 passed，`rtk npm test -- src/settings/SettingsDialog.test.tsx` 为 13 passed，`rtk npm test` 为 264 passed，`rtk npm run build` 通过，`rtk git diff --check` 通过。Spec review 和 code quality review 均已通过；跨 root 旧异步结果回写仍是基线既有竞态，后续如继续优化可统一引入 root token/cancellation 处理。

优化 PR #25 已完成第八段树干瘦身：分支 `codex/app-root-settings-hook`，PR `https://github.com/1572817043/mutiChrome/pull/25`，commit `e2f9258`。本轮只把 `src/App.tsx` 中 Settings 根目录、Chrome 路径和主题草稿相关状态与 handler 抽到 `src/settings/useRootSettings.ts`；`SettingsDialog` props 形状不变，App 继续负责 committed `rootPath`、profiles/projects/settings、启动会话和数据安全恢复落地。`App.tsx` 从 3130 行降到 3008 行，新 hook 216 行。新增 `src/settings/useRootSettings.test.tsx` 8 条 hook 级测试，覆盖草稿重置、加载根目录同步、persist 后 profileCount/browserPath 同步、数据安全恢复同步、同 root 保存顺序、跨 root 保存写入/commit 顺序、reveal 使用 committed rootPath 和 hook 返回值收窄。阶段验证通过：`rtk npm test -- src/settings/useRootSettings.test.tsx` 为 8 passed，`rtk npm test -- src/App.test.tsx -t "设置|根目录|Chrome|备份"` 为 26 passed，`rtk npm test -- src/settings/SettingsDialog.test.tsx` 为 13 passed，`rtk npm test` 为 272 passed，`rtk npm run build` 通过，`rtk git diff --check` 通过。Spec review 和 code quality review 第二轮均已通过；旧实现已有的连续保存乱序、跨 root Chrome 探测失败同步顺序和保存事务职责分界仍保留为后续独立行为优化，不在本结构优化 PR 改变。

优化 PR #26 已完成第九段树干瘦身与导入/恢复竞态收口：分支 `codex/app-profile-import-hook`，PR `https://github.com/1572817043/mutiChrome/pull/26`，commit `f9a46f7`。本轮把 `src/App.tsx` 中 profile 扫描导入的 UI 状态、候选选择、来源路径草稿和导入触发抽到 `src/profiles/useProfileImport.ts`，新增 hook 测试 14 条；同时补上 profiles 文档写入串行队列，保存时对 profiles/settings/projects/urlLibrary/recentUrls 做三方合并，避免 stale 保存回滚并发新增项目、网址库、最近网址或恢复文档。恢复流程改为轻量/完整恢复统一互斥，并把完整恢复与导入复制放入同一文档队列；后端恢复成功即视为落盘提交，前端不再二次保存，Chrome/root 检测失败只作为非致命提示，避免磁盘与 UI 分叉。Rust 导入目录改为先独占创建目标目录，目标已存在时直接拒绝且不删除既有目录；复制失败时报告 cleanup 失败。`App.tsx` 当前 3508 行，新 hook 234 行，`useDataSafetySettings.ts` 492 行，`profile_store.rs` 2567 行。阶段验证通过：`rtk npm test` 为 309 passed，`rtk npm run build` 通过，`rtk cargo test` 为 86 passed，`rtk cargo fmt --check` 通过，`rtk git diff --check` 通过。两轮只读子代理复审最终均 APPROVED；剩余非阻塞优化是实体 ID 复用遇到 stale 操作时可用不可复用 UID 或 `createdAt` replacement guard 继续加固。

优化 PR #27 已完成第十段稳定性加固：分支 `codex/entity-replacement-guard`，PR `https://github.com/1572817043/mutiChrome/pull/27`，commit `2b642ff`。本轮不加新功能、不改 Rust/Tauri command/UI/CSS、不引入不可复用 UID schema，先用现有 `createdAt` 做 replacement guard，防止 profile、project、urlLibrary 删除后复用同 ID 时，旧启动、旧项目打开、旧网址编辑草稿或旧删除确认串写到新实体。新增 `src/domain/profileDocumentMutationModel.ts` 承载原 `App.tsx` 内 document queue 三方合并逻辑，并补 `src/domain/profileDocumentMutationModel.test.ts` 5 条纯模型测试，覆盖 legacy 空 `createdAt` 与非空 replacement、project/urlLibrary replacement 保持 current 顺序、不被 stale requested 顺序拖动；`App.test.tsx` 新增 3 条集成测试覆盖复用同 ID 的新账号、新项目和新网址不被旧操作回写。阶段验证通过：`rtk npm test` 为 317 passed，`rtk npm run build` 通过，`rtk cargo test --manifest-path src-tauri/Cargo.toml` 为 86 passed，`rtk cargo fmt --manifest-path src-tauri/Cargo.toml -- --check` 通过，`rtk git diff --check` 通过。Spec review 和 code quality review 复审均 APPROVED；`rtk cargo test` 在仓库根目录因无 `Cargo.toml` 不适用，已改用显式 manifest 路径验证。

优化 PR #28 已完成第十一段树干瘦身：分支 `codex/profile-document-mutations-hook`，PR `https://github.com/1572817043/mutiChrome/pull/28`，commit `cadcb06`，merge commit `5eefabe`。本轮不加新功能、不改 UI/CSS/Rust/API，把 `App.tsx` 内 profiles document 的 mutation queue、generation/root guard、三方合并、save/commit 边界抽到 `src/domain/useProfileDocumentMutations.ts`；`App.tsx` 保留 import/restore 的业务编排和 UI 清理，只通过 hook 获取当前 document snapshot、串行队列和提交入口。新增 `src/domain/useProfileDocumentMutations.test.tsx` 6 条 hook 级测试，覆盖并发 persist 合并、同 root 旧 props rerender 不覆盖已提交 refs、root generation 变更后旧 persist 不保存不提交、旧 base 不复活已删除账号，以及复用同 ID 的新实体不被旧操作回写。阶段验证通过：`rtk npm test` 为 323 passed，`rtk npm run build` 通过，`rtk cargo test --manifest-path src-tauri/Cargo.toml` 为 86 passed，`rtk git diff --check` 通过。合并前只读 code review 通过，无 findings；GitHub PR checks 未配置，本地验证作为主门禁。

优化 PR #29 已完成第十二段树干瘦身：分支 `codex/profile-document-store-hook`，PR `https://github.com/1572817043/mutiChrome/pull/29`，commit `b450136`，merge commit `e363763`。本轮不加新功能、不改 UI/CSS/Rust/API，在 PR #28 的 mutation hook 外新增 `src/domain/useProfileDocumentStore.ts` facade，把 `rootPath/settings/profiles/projects` store state 和 mutation hook 接线从 `App.tsx` 移出；`App.tsx` 仍保留 loaded root、import queue、restore 落地、业务级 `persist` 包装、settings 同步、selectedIds 清理和 message 更新。新增 `src/domain/useProfileDocumentStore.test.tsx` 5 条 hook 级测试，覆盖初始 state 暴露、commit 后 state/外部回调同步、replace 后旧 persist 不保存不提交、`setRootPath` old→new→old 仍阻断旧 persist，以及 setter 更新后 state 与 snapshot 一致。阶段验证通过：`rtk npm test` 为 328 passed，`rtk npm run build` 通过，`rtk cargo test --manifest-path src-tauri/Cargo.toml` 为 86 passed，`rtk git diff --check` 通过。Luna spec review 首轮发现 `setRootPath` 未 bump generation 的中等风险，已补红绿测试和修复；复审 PASS，代码质量审查 PASS。

优化 PR #30 已完成第十三段树干瘦身：分支 `codex/remove-redundant-document-setters`，PR `https://github.com/1572817043/mutiChrome/pull/30`，commit `4756b35`，merge commit `4779809`。本轮不加新功能、不改 UI/CSS/Rust/API，只删除 `commitLoadedRoot` 和 `restoreDataSafetyDocument` 中紧跟 `replaceProfileDocumentState` 后的重复 `setRootPath/setSettings/setProfiles/setProjects`，并移除不再使用的 setter 解构；`syncLoadedRoot`、`syncRestoredRoot`、launch events、运行状态刷新、message 和 UI 清理保持不变。阶段验证通过：`rtk npm test -- src/domain/useProfileDocumentStore.test.tsx src/domain/useProfileDocumentMutations.test.tsx` 为 11 passed，`rtk npm test -- src/App.test.tsx -t "设置|根目录|恢复|导入|stale|复用同 ID"` 为 50 passed，`rtk npm run build` 通过，`rtk npm test` 为 328 passed，`rtk cargo test --manifest-path src-tauri/Cargo.toml` 为 86 passed，`rtk git diff --check` 通过。Luna 只读代码质量审查 PASS，无 findings。

优化 PR #31 已完成第十四段树干瘦身：分支 `codex/profile-document-store-persist`，PR `https://github.com/1572817043/mutiChrome/pull/31`，commit `e425634`，merge commit `0b35b09`。本轮不加新功能、不改 UI/CSS/Rust/API，把 `App.tsx` 本地 `persist(...)` 薄包装移入 `src/domain/useProfileDocumentStore.ts`，暴露为 `persistProfileDocument`，App 仅通过 alias `persist` 保持调用点不变；默认 settings/projects/rootPath 和 `baseDocument` 均保留调用方 render 快照语义，避免旧 persist 在切 root 后绑定新 root。新增/增强 `src/domain/useProfileDocumentStore.test.tsx` 4 条 facade 层测试，覆盖并发 persist 合并、setter 后默认 settings/projects、root 切换后旧 persist 不保存不提交，以及 `shouldCommit=false` 不保存不提交。阶段验证通过：`rtk npm test -- src/domain/useProfileDocumentStore.test.tsx src/domain/useProfileDocumentMutations.test.tsx` 为 15 passed，`rtk npm test -- src/App.test.tsx -t "设置|根目录|恢复|导入|stale|复用同 ID|创建|删除|保存"` 为 68 passed，`rtk npm run build` 通过，`rtk npm test` 为 332 passed，`rtk cargo test --manifest-path src-tauri/Cargo.toml` 为 86 passed，`rtk git diff --check` 通过。Luna 只读审查首轮发现默认参数使用可变 ref 会破坏 stale root 防护，已补红绿测试并修复；复审 PASS。

优化 PR #32 已完成第十五段树干瘦身：分支 `codex/name-document-ui-cleanup`，PR `https://github.com/1572817043/mutiChrome/pull/32`，commit `d4e240c`，merge commit `388f6ec`。本轮不加新功能、不改 UI/CSS/Rust/API，只在 `App.tsx` 内把 loaded root 后的 UI 清理命名为 `resetUiForLoadedRoot()`，把 data safety restore 后的 UI 清理命名为 `resetUiForRestoredDocument()`；两者清理范围保持各自差异，不合并、不改顺序。阶段验证通过：`rtk npm test -- src/App.test.tsx -t "设置|根目录|恢复|导入|stale|复用同 ID|创建|删除|保存"` 为 68 passed，`rtk npm run build` 通过，`rtk npm test` 为 332 passed，`rtk cargo test --manifest-path src-tauri/Cargo.toml` 为 86 passed，`rtk git diff --check` 通过。Luna 只读代码质量审查 PASS，无 findings；`docs/project-manager-handoff.md` 仍是既有未跟踪文件，本轮未触碰。

优化 PR #33 已完成第十六段树干瘦身：分支 `codex/profile-document-restore-facade`，PR `https://github.com/1572817043/mutiChrome/pull/33`，commit `f2a9674`。本轮不加新功能、不改 UI/CSS/Rust/API，把 `restoreDataSafetyDocument` 内的 document mutation queue、restore 前后 root guard 和 `replaceProfileDocumentState` 下沉到 `src/domain/useProfileDocumentStore.ts` 的 `restoreProfileDocument` facade；`App.tsx` 继续负责 `syncRestoredRoot`、恢复后的 UI 清理和 message。新增/增强 `src/domain/useProfileDocumentStore.test.tsx` 3 条 facade 层测试，覆盖恢复成功更新 document state 并返回原结果、target root 不匹配时不调用 restore、restore 等待期间 root 切换时返回 `null` 且不提交 stale 结果。阶段验证通过：`rtk npm test -- src/domain/useProfileDocumentStore.test.tsx src/domain/useProfileDocumentMutations.test.tsx` 为 18 passed，`rtk npm test -- src/App.test.tsx -t "设置|根目录|恢复|导入|stale|复用同 ID|创建|删除|保存"` 为 68 passed，`rtk npm run build` 通过，`rtk npm test` 为 335 passed，`rtk cargo test --manifest-path src-tauri/Cargo.toml` 为 86 passed，`rtk git diff --check` 通过。Luna 只读审查 PASS；残余风险是旧实现已存在的 `A -> B -> A` 同路径切换无法仅靠 root path 识别，不是本轮新增风险。`App.tsx` 当前 3138 行，`useProfileDocumentStore.ts` 204 行，`useProfileDocumentMutations.ts` 207 行；`docs/project-manager-handoff.md` 仍是既有未跟踪文件，本轮未触碰。

优化 PR #34 已完成第十七段树干瘦身：分支 `codex/profile-document-load-facade`，PR `https://github.com/1572817043/mutiChrome/pull/34`，commit `505aee9`。本轮不加新功能、不改 UI/CSS/Rust/API，把 `commitLoadedRoot` 中 loaded root 的 document state replacement 下沉到 `src/domain/useProfileDocumentStore.ts` 的 `loadProfileDocument` facade；`App.tsx` 继续负责 launch confirmation 清理、数据安全状态重置、loaded root 同步、launch events、message、运行状态刷新和 loaded root UI 清理。新增/增强 `src/domain/useProfileDocumentStore.test.tsx` 2 条 facade 层测试，覆盖 `loadProfileDocument` 一次性更新 rootPath/settings/profiles/projects 和 snapshot，以及 load 后 bump generation 使旧 persist 不保存不提交。阶段验证通过：`rtk npm test -- src/domain/useProfileDocumentStore.test.tsx src/domain/useProfileDocumentMutations.test.tsx` 为 20 passed，`rtk npm test -- src/App.test.tsx -t "设置|根目录|恢复|导入|stale|复用同 ID|创建|删除|保存"` 为 68 passed，`rtk npm run build` 通过，`rtk npm test` 为 337 passed，`rtk cargo test --manifest-path src-tauri/Cargo.toml` 为 86 passed，`rtk git diff --check` 通过。`App.tsx` 当前 3138 行，`useProfileDocumentStore.ts` 224 行，`useProfileDocumentMutations.ts` 207 行；`docs/project-manager-handoff.md` 仍是既有未跟踪文件，本轮未触碰。

优化 PR #35 已完成第十八段树干瘦身：分支 `codex/import-document-commit-plan`，PR `https://github.com/1572817043/mutiChrome/pull/35`，commit `eb0bde7`。本轮不加新功能、不改 UI/CSS/Rust/API，只把 `importCandidatesInQueue` 中导入成功后要保存/提交的 document 组装逻辑抽到 `src/domain/profileDocumentMutationModel.ts` 的 `buildImportDocumentCommitPlan` 纯函数；导入复制 loop、取消判断、rollback、异常处理、stale 判断、save/commit 调用、message 和编辑第一个导入账号仍留在 App 原流程。新增/增强 `src/domain/profileDocumentMutationModel.test.ts` 2 条纯模型测试，覆盖导入账号追加、`settings` normalizer、`version: 1` document 生成，以及项目 `profileIds` 只保留当前和本次导入后仍存在的账号 ID。阶段验证通过：`rtk npm test -- src/domain/profileDocumentMutationModel.test.ts` 为 7 passed，`rtk npm test -- src/App.test.tsx -t "导入|stale|复用同 ID|根目录|保存"` 为 46 passed，`rtk npm run build` 通过，`rtk npm test` 为 339 passed，`rtk cargo test --manifest-path src-tauri/Cargo.toml` 为 86 passed，`rtk git diff --check` 通过。`App.tsx` 当前 3129 行，`profileDocumentMutationModel.ts` 441 行；`docs/project-manager-handoff.md` 仍是既有未跟踪文件，本轮未触碰。

V4.0 已完成窗口编排基础第一段：新增 `src/browserWindows.ts` 纯模型，覆盖窗口注册表、`grid` 布局计划、屏幕容量计算、平铺坐标生成和窗口边界容差判断。现有“平铺窗口”已改为先把扫描结果转换为注册表，再生成布局计划并执行原有窗口移动命令；这一步只整理窗口资源和布局边界，不做实时群控、键鼠同步、标签页同步或网页状态同步。V4.0 已用 Computer Use 在真实 macOS 桌面完成补充测试：用户已开启 MultiChrome 辅助功能权限，软件已打开且已有两个 Chrome profile 窗口；MultiChrome 选中 `账号1` 和 `账号 2` 后，“检查窗口”读到两个账号各 1 个窗口，坐标分别为 `864x1017 @ 0,38` 与 `864x1016 @ 864,38`；点击“平铺窗口”后按钮进入“平铺中”，最终提示“已平铺 2 个窗口”，复查坐标仍为左右分屏。

V4.1 已完成同步布局接入窗口注册表：PR #3 `https://github.com/1572817043/mutiChrome/pull/3`，commit `1e58ec7d536e29181d391b2b2a9c243832598128`。本轮新增 `buildWindowLayoutSyncPlan` 纯模型，建模 source/target 的 `missing`、`minimized`、`window-error` 状态；source 不进入 `placements`，`noWindowCount`、`minimizedCount`、`failedCount` 只统计目标账号；目标窗口读取失败时跳过并继续同步其它目标。Claude 改代码前反证审查有效，采纳 `sourceWindowError`、最小化保护、目标失败不中断等约束；Claude 提交前 diff 红队审查有效，采纳补 App 层“目标读取失败但其它目标继续同步”测试。自动验证全部通过：`rtk npm test -- --run src/App.test.tsx -t "布局同步会跳过读取失败的目标并继续同步其它目标"`、`rtk npm test -- --run src/App.test.tsx -t "同步布局|布局同步"`、`rtk npm test`、`rtk npm run build`、`rtk cargo test --manifest-path src-tauri/Cargo.toml`、`rtk git diff --check`。本轮未做 macOS 辅助功能实机验证，因为未改底层 AX/Rust 窗口 API、`list_profile_windows`、`set_profile_window_bounds`、窗口 bounds 写入和读回机制；真实窗口同步继续纳入后续使用观察。

V4.3 已完成窗口操作 summary 标准化：PR #4 `https://github.com/1572817043/mutiChrome/pull/4`，commit `ca17053feedf0abf3cd8cb1bdcdfb8811a1c2543`。本轮标准化检查窗口、平铺窗口、同步布局的 operation summary；新增 `WindowOperationSummary`、inspect/tile/sync-layout summary builder、formatter 和 type guard，标准 summary 带 `summaryType: "window-operation"`；`OperationList` 优先展示标准窗口 summary，旧 summary fallback 保留；`App.tsx` 仅替换三类窗口操作 summary 写入，未改 toast 文案、operation status、窗口执行流程或 Rust。Claude 改代码前反证审查有效；提交前 diff 红队审查有效，采纳“检查窗口失败”detail、容量超限 `skippedCount` 补算 `noWindowCount`、`isWindowOperationSummary` action/可选字段类型校验、reason/label 分支防御处理和 tile `unchangedCount` 测试。阶段验证执行 `rtk npm test -- --run src/browserOperations.test.ts`、`rtk npm test -- --run src/browser/operations/OperationList.test.tsx`、`rtk npm test -- --run src/App.test.tsx -t "检查窗口|平铺窗口|同步布局|布局同步"`、`rtk npm test`、`rtk npm run build`、`rtk cargo test --manifest-path src-tauri/Cargo.toml`、`rtk git diff --check`：全部通过。本轮未做 macOS 辅助功能实机验证，因为未改底层 AX/Rust 窗口 API、坐标换算、bounds 写入/读回或新增窗口入口；按窗口类 PR 验证规则，本轮实机验证可选。

V4.2 路线决策：当前 main commit 为 `55b5561a02e5e6a5b3e7011f39c45c466da26dd8`；V4.1 已完成同步布局接入窗口注册表，V4.3 已完成窗口 operation summary 标准化。重新评估后，V4.2 布局预设暂缓，不实现。理由是当前 `grid` 已够 MVP 使用，2 窗口场景天然就是左右分屏，同步布局已经覆盖“手动摆出形状后复用”的真实需求；如果 V4.2 加 UI，会增加批量栏复杂度；如果只做纯模型，短期用户价值不足。后续只有真实使用证明 `grid` 不够时，再重新评估 V4.2；若未来要做，优先只做轻量“本次 preset”，不持久化、不做复杂 UI、不做多屏、比例或拖拽布局。

CDP 控制 spike 已完成：PR #8 `https://github.com/1572817043/mutiChrome/pull/8`，commit `b3cecdcf7e89389f33548814b75ffdc8fcb7167d`。本轮只新增实验目录 `experiments/cdp-control/`，未接入主应用，未修改 `src/`、`src-tauri/`，未新增第三方依赖，只使用 Node 内置 `fetch`、`WebSocket`、`node:test` 等能力。实验结果通过：3 个独立 Chrome profile 可同时绑定不同 `remote debugging port`；CDP 可 `list tabs`、读取 URL/title、navigate、click selector、type text；selector 缺失的 profile 会失败，但不会阻塞其它 profile。实验未访问 X/Galxe/Zealy 或其它真实平台，未处理密码、钱包、签名、私钥、助记词。架构结论：后续群控/自动化主路线可以优先 CDP；macOS Accessibility 继续作为窗口排列/前置能力，不作为网页动作同步主干；未来主应用若要支持 CDP，需要在启动 profile 时分配 `remote debugging port`；已普通方式启动且没有 `remote debugging port` 的 profile，通常不能无损接入 CDP，需要提示用户重新打开 profile；Windows 路线应优先复用 CDP 网页控制逻辑，只在 Chrome 启动路径和窗口管理上做平台适配。本 spike 明确不是产品化群控、不是自动化任务库，不做 X/Galxe/Zealy/真实网站自动化，不做实时鼠标键盘同步，不处理敏感凭据或钱包动作。

Browser Runtime PR 1 已完成实现并进入 PR：分支 `codex/browser-runtime-launch-snapshot`，PR #10 `https://github.com/1572817043/mutiChrome/pull/10`，commit `f8059edd12606d36301a3f1e1440d5b444e78312`。本轮范围是把新启动 profile 接入 Browser Runtime 最小闭环：未运行 profile 启动时分配本地 debug port，并把 `snapshot_browser_sessions` 扩展为返回 `debugPort`、`cdpStatus` 和 `runtimeError`；已运行但无 debug port 的 profile 只通过 snapshot 报告 `missing-port`，不自动重启、不自动杀进程。本轮不做 `list tabs`、navigate、click、type、群控 UI 或自动化任务库，不处理钱包、密码、签名、私钥或助记词。阶段验证通过：`rtk cargo test --manifest-path src-tauri/Cargo.toml` 为 Rust 50 passed，`rtk npm test` 为前端 230 passed，`rtk npm run build` 通过，`rtk npm run tauri:build` 通过，`rtk git diff --check` 通过。`stash@{0}` 仍保留且未恢复。

Browser Runtime PR 2 已完成实现并进入 PR：分支 `codex/browser-runtime-list-tabs`，PR #11 `https://github.com/1572817043/mutiChrome/pull/11`，commit `49ce712629b1a7bf9b61f8662883defbcf6791ac`。本轮范围是新增只读 `list_runtime_tabs` Tauri command 和前端 `profileApi.listRuntimeTabs`，通过 CDP `/json/list` 读取当前 profile 的 page tab，并返回 `targetId`、`type`、`url`、`title`、`webSocketDebuggerUrl` 和 `checkedAt`。本轮不做 UI、navigate、click、type、WebSocket session、群控或自动化任务库。阶段验证通过：`rtk cargo test --manifest-path src-tauri/Cargo.toml` 为 Rust 65 passed，`rtk npm test` 为前端 230 passed，`rtk npm run build` 通过，`rtk npm run tauri:build` 通过，`rtk git diff --check` 通过。实机只读验证通过：`account-005` 以 debug port `19222` 启动，`/json/list` 返回 1 个 page tab，包含 `id`、`url`、`title` 和 `webSocketDebuggerUrl`。`stash@{0}` 仍保留且未恢复。

Browser Runtime PR #12 已完成实现并进入 PR：分支 `codex/browser-runtime-dev-diagnostics`，PR #12 `https://github.com/1572817043/mutiChrome/pull/12`，commit `6390b78e5a29e93d7b3b532a2fdedf022ac76b10`。本轮范围是在 Settings 底部新增 DEV-only 折叠“开发诊断”，单选账号时可只读调用 `listRuntimeTabs`，展示 `cdpStatus`、`debugPort`、tab 数量、`title`、`url` 和短 `targetId`；不展示完整 `webSocketDebuggerUrl`，关闭 Settings 后旧请求不会污染结果。本轮不做 release 正式 UI、navigate、click、type、WebSocket session、群控或自动化任务库。阶段验证通过：SettingsDialog targeted tests 11 passed，App targeted tests 3 passed，`rtk npm test` 为 239 passed，`rtk npm run build` 通过，`rtk git diff --check` 通过；spec review、code quality review 和 Claude diff red-team 均无阻断问题。`stash@{0}` 仍保留且未恢复。

Browser Runtime PR #13 已完成实现并进入 PR：分支 `codex/browser-runtime-navigate-tab`，PR #13 `https://github.com/1572817043/mutiChrome/pull/13`，commit `776cee1e4d4b3364016ba26e815f0edc03ef5d61` 和 `d65b4b2c7b706aa3a1ef04f1aa079d005190f8bc`。本轮范围是在 DEV-only Settings “开发诊断”里新增 `navigate_runtime_tab` 最小能力：单选账号、手动输入 `http/https` URL，对第一个 `type=page` 且有 `webSocketDebuggerUrl` 的标签页执行 CDP `Page.navigate`；后端清洗对外错误，不泄露完整 `ws://.../devtools/page/...`，WebSocket 连接、握手、发送、响应读取共享总 deadline，关闭 Settings、切换账号或切换根目录后的旧请求不会污染 UI。新增前端 `profileApi.navigateRuntimeTab`、Settings 导航输入和反馈、Rust WebSocket CDP helper 及相关红绿测试。本轮不做批量、群控、click/type、WebSocket 长连接 session、任务持久化、正式 Runtime 控制台、真实平台自动化，也不处理密码、钱包、签名、私钥或助记词。阶段验证通过：`rtk cargo test --manifest-path src-tauri/Cargo.toml` 为 Rust 83 passed，`rtk npm test` 为前端 248 passed，`rtk npm run build` 通过，`rtk cargo check --manifest-path src-tauri/Cargo.toml --locked --all-targets` 通过且无 warning，`rtk npm run tauri:build` 通过，`rtk git diff --check` 通过。Spec review 和 code quality review 均已通过；Claude 提交前 diff 红队有效，采纳补 WebSocket URL 防御分支测试。剩余风险：本轮未做真实 Chrome 端到端导航实机验证，导航后立即刷新 tabs 可能短暂读到旧 URL/title。

Browser Runtime PR #14 已完成真实导航修复并进入 PR：分支 `codex/browser-runtime-navigate-real-fix`，PR #14 `https://github.com/1572817043/mutiChrome/pull/14`，commit `17e23cffd8bd7e432faf5f9f1079d4c7bf67463a`。真实 DEV App 验收发现 PR #13 的 `/json/list` 请求头写死 `Host: 127.0.0.1`，Chrome 返回的 `webSocketDebuggerUrl` 缺少 debug port，导致后续 WebSocket 连接落到默认 80 端口并显示“CDP 导航失败”。本轮把 `fetch_cdp_tabs` 的 Host 改为 `127.0.0.1:{port}`，并新增回归测试捕获请求头，断言恰好一个动态 Host 行；测试 helper 改为循环读取完整 HTTP header，避免 TCP 分包导致偶发失败。Computer Use 真实回归通过：DEV App 启动账号1，debug port `19222`，导航前 `chrome://newtab/`，点击“导航标签页”后成功文案为“已导航第一个 page 标签页：https://example.com”，复读结果为 `Example Domain / https://example.com/`；非法 URL 仍提示“请输入有效的 http:// 或 https:// URL”，未选账号时读取、输入和导航控件禁用。阶段验证通过：`rtk cargo test --manifest-path src-tauri/Cargo.toml` 为 Rust 84 passed，`rtk npm test` 为前端 248 passed，`rtk npm run build` 通过，`rtk cargo check --manifest-path src-tauri/Cargo.toml --locked --all-targets` 通过，`rtk npm run tauri:build` 通过，`rtk cargo fmt --manifest-path src-tauri/Cargo.toml -- --check` 通过，`rtk git diff --check` 通过。Spec review 通过；code quality review 曾要求修复测试分包风险，已补丁并复审通过。

Browser Runtime PR #15 已完成导航后确认刷新并进入 PR：分支 `codex/browser-runtime-navigate-confirmation`，PR #15 `https://github.com/1572817043/mutiChrome/pull/15`，commit `282aa8bc8a76b070e9a80acd74f9e6f29e647ecc`。本轮只稳定 DEV-only Settings “开发诊断”的导航后刷新：`navigateRuntimeTab` 成功后最多 3 次、每次间隔 100ms 轮询 `listRuntimeTabs`，直到同一 `targetId` 的 URL 与目标 URL 匹配；`https://example.com` 与 `https://example.com/` 视为匹配。确认成功后显示最新 tabs；未确认时不把导航判失败，保留成功文案、展示最后一次 tabs，并提示“导航成功，但暂未确认标签页已更新。”；刷新接口报错时保留既有组合错误；关闭 Settings、切换账号、切换根目录和 App 卸载后的旧请求不会污染 UI。本轮不做 click/type、群控、正式 Runtime 控制台或真实平台自动化。TDD 覆盖首次旧 URL 后二次确认、非目标首 tab 同 URL 不误确认、未确认正好 3 次轮询、卸载中断旧轮询。Computer Use 真实 DEV 回归通过：debug port `19222`，导航前 `chrome://newtab/`，导航后 UI 显示 `Example Domain https://example.com/`，未出现未确认提示，非法 URL 仍显示 http/https 校验错误。阶段验证通过：`rtk npm test` 为前端 250 passed，`rtk cargo test --manifest-path src-tauri/Cargo.toml` 为 Rust 84 passed，`rtk npm run build` 通过，`rtk cargo check --manifest-path src-tauri/Cargo.toml --locked --all-targets` 通过，`rtk npm run tauri:build` 通过，`rtk cargo fmt --manifest-path src-tauri/Cargo.toml -- --check` 通过，`rtk git diff --check` 通过。Spec review 通过；code quality review 曾要求收紧 targetId、卸载 cleanup 和轮询次数断言，已补丁并复审通过。

Settings PR #16 已完成 release 冒烟后发现的根目录草稿修复并进入 PR：分支 `codex/settings-root-path-draft-discard`，PR #16 `https://github.com/1572817043/mutiChrome/pull/16`，commit `fd41f54fb99622d327695cf656b6a49b0487b748`。release 包冒烟确认非 DEV 下不显示“开发诊断”、账号启动和运行状态正常，同时发现“配置根目录”未保存输入关闭后再打开仍停留在 UI；本轮把 Settings 的配置根目录改为 `rootPathDraft`，关闭设置会丢弃未检测/未保存草稿，只有点击“检测”或“保存设置”才应用。空白根目录会在调用 root API 前拦截；跨根保存先只读准备目标根数据，保留目标 profiles/projects/其他 settings，只合并当前 Chrome 路径和主题草稿，`saveProfiles` 成功后才提交 UI 根状态；保存失败不会切换当前根或清空选择/健康/备份上下文；保存成功后 Chrome 检测失败也不会回滚新根。本轮不改变 Chrome 路径/主题既有关闭丢弃语义，不把 rootPath 写入 `profiles.json` 的 settings。TDD 覆盖关闭丢弃根目录草稿、空白检测/保存拦截、跨根保存失败保留上下文、跨根保存成功合并目标数据、Chrome 检测失败不回滚新根，以及 rootPath 真正检测切换后 Browser Runtime 旧请求 guard。阶段验证通过：`rtk npm test` 为前端 257 passed，`rtk cargo test --manifest-path src-tauri/Cargo.toml` 为 Rust 84 passed，`rtk npm run build` 通过，`rtk cargo check --manifest-path src-tauri/Cargo.toml --locked --all-targets` 通过，`rtk npm run tauri:build` 通过，`rtk cargo fmt --manifest-path src-tauri/Cargo.toml -- --check` 通过，`rtk git diff --check` 通过。Spec review 和 code quality review 均已通过；code quality review 曾要求拆掉保存前有副作用的 `loadRoot`，已改为 `readRootData` + `commitLoadedRoot` 并复审通过。

Settings PR #17 已完成设置根目录草稿测试整理并进入 PR：分支 `codex/settings-test-cleanup`，PR #17 `https://github.com/1572817043/mutiChrome/pull/17`，commit `a09a86a3ea907f5dd85fa9e2a8f0ad48f3861254`。本轮不改生产代码，只在 `src/App.test.tsx` 提取打开设置、修改配置根目录草稿和点击“检测”的测试 helper，保留根目录草稿与 Browser Runtime 旧请求 guard 的核心断言。阶段验证通过：设置相关定向测试 18 passed，开发诊断 rootPath guard 定向测试 1 passed，`rtk npm test` 为前端 257 passed，`rtk npm run build` 通过，`rtk git diff --check` 通过；只读测试清理审查通过。

后续路线已明确：V4 继续打窗口编排基础，不直接做完整群控。V4.4 做轻量窗口面板。V5 才进入同步控制器基础，先做 `sync-bounds`、dry-run 预览和结果详情；鼠标、键盘、滚动、标签页等交互同步只在 V5.4 做技术预研，不进 MVP 主流程。

## 稳定观察 backlog

当前状态：V4.1 已完成同步布局接入窗口注册表，V4.3 已完成窗口 operation summary 标准化，V4.2 已评估为暂缓；V4.4 和 V5 暂不进入。当前阶段进入真实使用观察期，后续只处理真实使用中出现的问题和已知技术债，不立即开工新增能力。

观察优先级：

- P0：真实数据风险，例如备份、恢复、导入、删除异常。
- P1：核心工作流 bug，例如账号启动、批量打开、项目打开、窗口同步或平铺异常。
- P2：体验不清，例如操作记录、错误文案、按钮状态。
- P3：技术债，例如 `App.tsx` 仍 3000+ 行、`SettingsDialog`/`BulkActionBar` props 过多、`api.ts` normalize 仍在 API 层、`styles.css` 偏大、Rust 文件偏大。

后续规则：没有复现步骤，不开修复 PR；一个问题一个小分支；不继续大重构；不做 V4.2、V4.4 或 V5，除非有真实使用证据；不做代理、指纹、自动任务、AI 助手或群控；只有窗口底层改动才强制 macOS 实机验证。

## 当前 release 真实使用验收

当前 main commit 为 `98349cd7524742d26634f7249f508166907f2077`，release App 已重新打包，路径为 `src-tauri/target/release/bundle/macos/MultiChrome.app`，App 修改时间为 `2026-07-26 16:59:21 +0800`。自动验证结果：`rtk npm test` 230 tests passed，`rtk npm run build` 通过，`rtk cargo test --manifest-path src-tauri/Cargo.toml` 42 passed，`rtk npm run tauri:build` 通过，`codesign --verify --deep --strict --verbose=2` 通过。用户已完成真实使用测试，未发现问题；当前 release 可用于真实使用观察。当前仍保留 `stash@{0}`，未恢复。后续进入小问题修复通道，只有真实复现问题才开 PR。

## 已确认决策

- 第一版只做多账号 Chrome profile 管理。
- 先做 macOS 桌面 App，后期再适配 Windows。
- 技术栈使用 Tauri + React + TypeScript + Vite。
- 开发期 profile 根目录先放本机。
- 账号元数据使用 JSON，且放在配置根目录下，方便迁移。
- 浏览器路径设置保存在 `profiles.json` 的 `settings.browserPath` 中，旧版无 settings 的文档会自动使用默认路径。
- 常用网址和最近网址保存在 `profiles.json` 的 `settings.favoriteUrls`、`settings.recentUrls` 中，会跟随配置根目录迁移。
- 网址库保存在 `profiles.json` 的 `settings.urlLibrary` 中，用于结构化管理常用网址的名称、URL、标签和备注；主界面采用 Notion database 风格表格，新建/编辑用弹窗草稿，保存才写入；`favoriteUrls` 保留为兼容批量打开的小快捷入口，并由网址库派生同步。旧版 `favoriteUrls` 会自动迁移成网址库条目。
- 项目保存在 `profiles.json` 的 `projects` 数组中，表示“多个入口网址 + 一批绑定账号 + 打开间隔 + 备注”，用于长期重复打开的空投/活动入口；它不是自动任务系统。
- 账号和项目编辑都采用草稿弹窗：字段修改不会实时写入，只有点“保存账号/保存项目”才保存；点 X、取消或点遮罩关闭都会丢弃本次草稿。
- 完整 profile 备份保存到配置根目录的 `app-data/backups/full-profiles-*` 文件夹，包含筛选后的 `profiles.json`、`manifest.json` 和对应 `profiles/account-xxx/`；恢复前必须先扫描预览，真正恢复时用独立确认弹窗，默认允许覆盖同 ID 的 profile 文件夹。
- AdsPower 里的“账号平台”能力先做成账号资料库：记录平台、登录网址、用户名、备注等信息；密码字段和网页自动识别输入框并自动填入属于更复杂的安全存储/浏览器扩展/自动化能力，已确认暂不进入当前阶段。
- UI 采用启动器式 AppShell：左侧导航、主区域账号卡片网格、编辑弹窗、设置弹窗。
- UI 方向参考 AdsPower 的生产力质感，但不照搬 AdsPower 的表格型主界面；用户更偏好长方形卡片/小方块启动器，主界面应保持简洁，低频信息放进编辑弹窗。
- 后期计划可加入软件内 AI 聊天助手，用于导入项目、整理链接、批量创建账号草稿和辅助配置，但现阶段先做好基础功能；AI 写入任何数据前都要预览确认。
- 后期计划可做窗口同步、群控等能力，但不进入当前 MVP。
- 窗口类 PR 验证规则：改 Rust Accessibility API、`list_profile_windows`、`set_profile_window_bounds`、窗口坐标换算、bounds 写入/读回确认机制或新增窗口操作入口时，必须做 macOS 实机验证；纯前端窗口分类模型、operation summary、文案/展示调整和测试补充，实机验证可选。
- 账号颜色保存为 `profiles.json` 中的 `accentColor`，跟随配置根目录迁移；老账号缺少颜色时按账号编号显示默认色。
- 外部图标区分的第一轮实验不改主应用，只放在 `experiments/launcher-apps/`；它能区分“启动入口”，但运行中的窗口仍归属 `/Applications/Google Chrome.app`，Dock 运行图标是否能完全区分需要后续独立 helper app 或独立浏览器 bundle 方案继续验证。
- 外部图标区分第二轮采用 stay-open helper：仍不复制 Chrome，只让账号 `.app` 自身保持运行，负责启动或聚焦对应 `--user-data-dir` 的 Chrome 进程。
- shell stay-open helper 被证实不能响应 Dock 点击；原因是 macOS 只会激活已运行 App，不会重新执行 shell 脚本。第二轮修正后改用 AppleScript applet，并通过 `on reopen` 处理 Dock 点击。
- 用户确认双图标体验不理想：身份图标会和 Google Chrome 原生图标一起出现；该路线暂时放下，当前不继续做 Dock 图标区分。
- 用户确认账号状态管理对当前 MVP 过重：主界面、编辑弹窗、筛选和批量操作中不再显示 `正常/待检查/归档`，主要依靠名称、备注和标签管理账号。

## 已完成

- 创建 `docs/plan.md`。
- 创建 `docs/project-overview.md`，用于新对话快速恢复项目背景、决策、踩坑记录和后续路线。
- 初始化 Tauri + React + TypeScript + Vite 项目。
- 使用 JSON 作为账号元数据格式。
- 实现账号模型：创建账号、更新账号、生成 `account-001` 风格 ID。
- 实现 Tauri 后端根目录能力：初始化目录、读写 `profiles.json`、计算目录大小、生成 profile 路径。
- 实现基础桌面管理 UI：TopBar、Sidebar、账号列表、详情面板。
- 优化 UI 第二版：压缩顶部工具栏、移动数据目录、表格化账号列表、改进空状态和右侧属性面板。
- 接入浏览器预览模式和 Tauri 桌面命令。
- 修复 macOS 打开 Chrome 时把 profile 目录当作本地文件夹页面打开的问题；现在使用 `--user-data-dir=/完整路径` 形式传参。
- 修复新建账号后未打开 Chrome 前 profile 文件夹不存在的问题；保存账号元数据时会同步创建对应目录。
- 实现 V0.2 基础管理：归档/恢复账号、只删除记录、删除记录和文件夹、复制账号、复制导入已有 profile、批量刷新目录大小。
- 修复删除按钮在 Tauri WebView 中依赖 `window.confirm` 导致无响应的问题；改为右侧应用内确认面板。
- 支持手动设置浏览器路径；未检测到浏览器时禁用“打开 Chrome”操作。
- 使用 `/Users/a0000/Downloads/mutiChrome.jpeg` 生成并替换 Tauri 图标资源：`icon.png`、`32x32.png`、`128x128.png`、`128x128@2x.png`、`icon.icns`、`icon.ico`。
- 通过前端测试、前端构建、Rust 测试和 Tauri release 构建。
- 实现 V0.3 启动器布局：去掉右侧 Inspector，主区域改为账号卡片；点击卡片启动 Chrome；点击卡片右上角编辑按钮打开编辑弹窗。
- 删除入口只保留在编辑弹窗底部危险区，并继续使用二次确认。
- 根目录、Chrome 路径和检测状态移入左下角设置弹窗。
- 增加前端交互测试，覆盖“点击卡片启动”和“点击编辑不启动且删除只在弹窗内出现”。
- 修复账号启动后强制打开 `about:blank` 的问题；后续普通卡片启动改为传 `chrome://newtab/`，用于重新唤起 profile 或在已运行时打开新标签。
- 实现 V0.4 账号辨识和基础批量管理：账号颜色、编辑颜色、标准/紧凑视图、多选、全选当前、批量追加标签。
- 优化中等宽度窗口下的工具栏布局：搜索区和全选/数量操作组分离，避免控件一项一行地散开。
- 旧版曾增加当前会话内的重复启动防护：同一账号已经启动后，再点卡片不会重复打开新的 Chrome 窗口；后续发现该策略无法感知用户 `Command+Q` 关闭 Chrome，已移除长期“已打开”记忆。
- 新增 `experiments/launcher-apps/generate-launcher-apps.mjs` 技术实验脚本，可生成 `MC-account-001.app` 这类账号启动器。
- 外部启动器实验脚本直接写 `.icns` 容器，不依赖 `iconutil`；每个启动器有独立 `CFBundleIdentifier`、独立图标颜色和号码角标。
- 已生成实验启动器：`.launcher-experiment/MC-account-001.app`、`.launcher-experiment/MC-account-002.app`。
- `experiments/launcher-apps/generate-launcher-apps.mjs` 新增 `--mode stay-open`，默认 `launch-once` 行为保持不变。
- 已生成第二轮实验包：`.launcher-helper-experiment/MC-account-001.app`、`.launcher-helper-experiment/MC-account-002.app`。
- stay-open helper 已从 pid 文件 + zsh 循环改为 AppleScript applet：通过 `osacompile -s` 编译为 stay-open App，使用 `on run`、`on reopen`、`on idle` 处理启动、Dock 点击和生命周期。
- AppleScript helper 生成后会重新写入自定义 `Info.plist` 和 `AppIcon.icns`，并执行本地 ad-hoc 签名，避免修改 plist/icon 后签名失效。
- 已停止当前运行中的实验 helper 进程，未关闭 Chrome profile 窗口。
- 新增配置根目录健康检查：可检查根目录、`app-data/profiles.json`、`profiles/`、登记账号目录缺失、空 profile 目录、未登记孤儿目录、非目录条目和索引损坏。
- 健康检查入口放在设置弹窗内，主界面保持简洁；检查只读展示，不自动修复或改动用户数据。
- 简化账号状态功能：移除主界面状态筛选、账号卡片状态角标、编辑弹窗状态按钮、归档按钮和批量改状态按钮；`profiles.json` 里的 `status` 字段暂不迁移，继续兼容旧数据。
- 新增轻量备份/恢复：设置弹窗内可创建 `profiles.json` 备份到 `app-data/backups/`，也可从指定备份 JSON 路径恢复账号索引和设置；恢复不会删除已有 profile 文件夹，只会补齐备份账号对应目录。
- 从备份恢复前新增二次确认，避免误触覆盖当前账号索引和设置。
- 设置弹窗新增“打开数据目录”和“打开备份目录”入口；打开备份目录时会先确保 `app-data/backups/` 存在。
- 健康检查新增“修复可自动处理项”：只自动创建缺失的根目录、`app-data/`、`profiles/`、`app-data/backups/`、空 `profiles.json` 和已登记账号缺失的 profile 文件夹；不会覆盖损坏索引，不会删除文件，不会自动登记孤儿目录。
- 健康检查新增孤儿 Profile 目录登记：发现 `profiles/` 下存在但 `profiles.json` 未登记的目录时，可手动点击“登记为账号”；登记只写入账号索引，不复制、不移动、不删除原目录。
- 新增“选中账号打开指定网址”：批量操作栏可输入网址，自动补 `https://`，并为选中的每个账号用独立 Chrome profile 打开该网址；普通点击卡片走账号自身的默认新标签启动。
- 修复账号卡片重复点击被误拦截的问题：普通卡片点击不再记录“本轮已打开”，只保留启动过程中的瞬时防抖；每次点击都会向同一 profile 发送启动请求，并默认传 `chrome://newtab/`，让 Chrome 在已运行时尽量打开/切到同一 profile 的新标签。
- 修复已运行账号重复点击容易重新弹出窗口的问题：后端启动命令改为按运行状态分流，未运行 profile 继续使用 `open -n -a Chrome --args --user-data-dir=...`，已运行 profile 直接调用 Chrome 包内可执行文件并传同一个 `--user-data-dir` 和目标 URL，让 Chrome 复用现有 profile 进程。若当前页本来就是新标签页，视觉上可能只是聚焦现有新标签；核心目标是不再重复开同账号窗口。
- 批量打开网址改为队列式启动：选中多个账号后按顺序逐个打开，不再同时全部启动；批量栏可设置 1-60 秒间隔，默认 3 秒，并支持中途停止。
- 批量打开栏新增常用网址和最近网址：常用网址可手动收藏/删除，最近网址会在批量打开成功后自动记录；点击网址短标签可快速回填，适合每天打卡、项目活动页、抽奖页等重复入口。
- 左侧“项目”模块已启用：可新建项目，设置名称、网址、备注、打开间隔和绑定账号；项目卡片可按绑定账号顺序批量打开该项目网址。典型场景是 Galxe/Zealy/项目官网/抽奖页等长期重复入口。
- 修正新建交互：新建账号和新建项目改为草稿弹窗，保存才写入 `profiles.json`；取消或点 X 不会留下未完成账号/项目。
- 账号支持批量新建：可在账号页一次粘贴多行账号文本，格式支持“名称, 标签, 备注”、“名称 | 标签 | 备注”和从表格复制的 Tab 分隔内容；保存后一次性创建多个账号记录和对应 profile 目录，取消或关闭不会落盘。
- 修复账号卡片右侧布局：未运行账号不再显示“切换到账号”按钮；大小状态文本如“未检测”固定在右侧按钮下方的状态槽，暗色模式和运行态两个按钮同时出现时也不再重叠。
- 精简账号卡片低频信息：账号卡片不再常驻显示目录大小，也移除右上角“刷新大小”；目录大小只在打开账号编辑弹窗时刷新当前账号一个 profile。后续如果需要容量统计，考虑做左侧独立统计模块，不塞回账号卡片。
- 补齐项目基础闭环：项目页支持搜索名称/网址/备注；编辑弹窗支持复制项目和删除项目并二次确认；项目打开队列支持中途停止。
- 实现账号平台资料库第一版：账号编辑弹窗可添加平台、登录网址、用户名、备注；可从该账号 profile 打开平台登录网址；可复制用户名；可删除平台资料。资料保存到 `profiles.json` 的 `profiles[].accountPlatforms`，跟随配置根目录迁移。
- 账号平台资料库新增常用模板：新增或展开平台资料后，可一键套用 X、Discord、Telegram、Gmail、Galxe、Zealy 的平台名和登录网址；模板只修改弹窗草稿，仍需点击“保存账号”才写入。
- 窗口管理 V1 开始：新增运行状态识别。macOS 后端通过 `ps` 扫描 Chrome 主进程的 `--user-data-dir=<root>/profiles/<account-id>`，去重后返回正在运行的账号 ID；前端在账号卡片显示“运行中”，启动成功后会临时标记为运行中。后续已改为账号页轻量动态刷新：前台每 5 秒刷新一次运行状态，窗口回到前台或页面恢复可见时也会刷新；该状态不写入 `profiles.json`。
- 窗口管理 V1 第二步：运行中的账号卡片新增“切换到账号”按钮。后端通过 `ps -axo pid=,command=` 找到对应 Chrome 主进程 PID，再用原生 macOS Accessibility API 把该进程窗口拉到前台；这是进程级切换，不承诺精确窗口或标签页。
- 窗口管理 V1 第三步：批量栏新增“检查窗口”。选中账号后可检查其中正在运行的账号当前能被 Accessibility API 看到的 Chrome 窗口数量；后端新增 `list_profile_windows`，枚举窗口标题、位置、尺寸和最小化状态，不持久化。
- 窗口管理 V1 第四步：窗口检查结果新增首个窗口的位置和尺寸，例如 `1280x720 @ 12,34`。后端通过 Accessibility API 读取窗口 `AXPosition` 和 `AXSize`，前端只作为诊断摘要展示。
- 窗口管理 V1 第五步：批量栏新增“平铺窗口”。选中账号后，只处理其中正在运行的账号；前端按当前屏幕可用宽高计算网格，后端新增 `set_profile_window_bounds` 设置该账号首个 Chrome 窗口的位置和尺寸。该能力是手动平铺小实验，不做同步、不保存布局、不移动未运行账号。
- 窗口管理 V1 第六步：平铺前新增窗口预检查。点击“平铺窗口”后会先读取每个选中运行账号的窗口列表，只把能读到至少一个窗口的账号纳入网格；没有可读窗口的账号会被跳过并计入提示，网格按实际可平铺账号数量重新计算，避免出现空洞布局。
- 窗口管理 V1 第七步：窗口类操作失败提示优化。“切换到账号”“检查窗口”“平铺窗口”遇到 Accessibility API、Apple Events 或权限相关失败时，会提示到 macOS 系统设置的辅助功能权限位置，并保留原始错误，避免只看到晦涩命令错误。
- 窗口管理 V1 第八步：平铺窗口尊重屏幕可用区域偏移。前端会读取 `screen.availLeft/availTop`，窗口网格从系统报告的可用区域左上角开始，而不是固定从 `0,0` 开始；字段不存在时回退到 `0`，保持旧环境兼容。
- 窗口管理 V1 第九步：平铺窗口新增最小可用尺寸保护。每个平铺窗口按至少 `320x240` 估算，如果当前屏幕容不下已选运行窗口数量，就不移动任何窗口，并提示减少选择或分批平铺，避免把大量账号压成不可用的小窗口。
- 窗口管理 V1 第十步：平铺窗口新增多窗口账号提示。仍然只移动每个账号的首个 Chrome 窗口；如果某个账号有多个可读窗口，平铺完成提示会说明有几个账号存在多个窗口、仅平铺首个窗口，避免误以为所有窗口都会被整理。
- 窗口管理 V1 第十一步：窗口检查新增最小化状态。后端读取窗口 `AXMinimized` 属性并序列化为 `minimized`；前端只在检查摘要里给首个窗口追加“已最小化”，不自动恢复、不移动窗口。
- 窗口管理 V1 第十二步：布局同步 V0。批量栏新增“主账号”选择和“同步布局”按钮；会读取主账号首个窗口的位置和尺寸快照，并应用到其它选中且正在运行、且有可读窗口的账号首个窗口。该能力只同步窗口位置和大小，不同步鼠标、键盘、滚动、标签页，也不自动打开未运行账号。
- 窗口管理 V1 第十三步：布局同步新增最小化保护。主账号首个窗口如果已最小化，会中止同步并提示先恢复窗口；目标账号首个窗口如果已最小化，会跳过该账号并计入提示，不自动恢复窗口、不设置 bounds。
- 修正账号编辑弹窗交互：顶部标题和关闭按钮固定，不再随内容滚动；底部保存按钮固定；已有账号编辑改为保存式草稿，关闭或点遮罩不会写入修改。
- 优化账号平台资料库展示：已有平台默认折叠成一行摘要，保留打开、复制、编辑、删除动作；点击编辑才展开字段；新增平台会自动展开，方便立即录入。
- 修正项目编辑交互：已有项目编辑改为保存式草稿，修改名称、网址、间隔、备注和绑定账号后，只有点击“保存项目”才写入；点 X、取消或遮罩关闭会丢弃草稿。
- 精简项目卡片：主界面项目卡片只保留项目名、网址、绑定账号数、打开间隔、开始打开和编辑；备注、账号列表和复制入口收进编辑弹窗。
- 修复项目编辑弹窗布局：项目表单、项目操作和危险操作统一放入可滚动内容区，底部保存栏固定，避免截图中保存按钮上浮和底部操作区断层。
- 统一账号/项目编辑弹窗的遮罩关闭行为：点击弹窗外部等同点右上角 X，只关闭并丢弃草稿，不会保存修改；同时补强测试，避免弹窗标题变化造成误判。
- 项目支持多个网址：每条网址可设置名称、URL 和备注；项目卡片保持简洁，只显示网址数量，多个网址时可选择打开全部或只打开某一个网址。
- 项目打开队列支持多网址：打开全部时会按“账号顺序”逐个账号处理，每个账号连续打开该项目的所有网址，然后再按项目间隔进入下一个账号。
- 项目网址列表支持批量导入、单条复制和上下排序：可把多行“名称 URL 备注”或纯 URL 一次导入为项目入口；每条网址可复制 URL，也可在编辑弹窗里调整顺序。
- 设置弹窗交互补齐：点击外部遮罩会关闭；关闭设置会丢弃未保存的 Chrome 路径草稿，并清除备份恢复确认框。
- 弹窗支持 `Esc` 关闭：账号编辑、项目编辑、批量新建账号和设置弹窗按 `Esc` 等同点右上角 X，不保存草稿。
- 账号删除确认改为独立小弹窗：点击“只删除记录”或“删除记录和文件夹”只打开确认窗口，点“确认删除”后才执行删除；确认内容不再挤在账号编辑弹窗内部。
- 账号导入升级为批量扫描导入：账号页“导入”先扫描来源目录并展示候选；支持识别旧 MultiChrome 根目录、普通 profiles 目录或整理好的来源目录；扫描只读，不写入、不复制；默认只勾选可信 profile，可疑项需手动勾选，跳过项不能导入。
- 批量导入采用复制导入，不做原地登记：确认后为每个候选生成新的 `account-xxx`，复制到当前根目录 `profiles/` 下，全部复制成功后才写入 `profiles.json`；如果中途失败，会删除本次已复制的新目录，不写入账号索引，方便回滚。
- 批量导入新增去重标记：导入成功后会在账号索引写入 `importSource`，并在目标 profile 根目录写 `.multichrome.json`；后续扫描时会根据来源路径或 marker 的 `profileUid` 标记“已导入”，重复候选默认禁用，避免同一个来源被反复复制。
- 批量导入预览提示已区分“可导入 / 可疑 / 已导入”：重复候选不会再被算进可导入数量，避免扫描结果和导入按钮状态不一致。
- 新增完整 profile 文件夹备份/恢复：设置弹窗可选择全部账号或当前选中账号，先预览账号数和预计大小，再创建完整备份目录；恢复时先扫描备份目录，展示新增/覆盖账号数量和总大小，再通过独立确认弹窗执行恢复。
- 完整备份恢复采用合并策略：备份内账号会写入当前账号索引，当前本机设置保持不被备份覆盖；同 ID profile 文件夹会先复制到 staging 目录，再替换目标目录，降低半截覆盖风险。
- 账号支持批量删除：选中账号后批量栏可打开“删除选中”确认弹窗；可选择“只删除记录”或“删除记录和文件夹”。删除文件夹模式会先逐个删除 profile 目录，全部成功后才移除账号索引；失败时保留账号索引并显示错误。
- 浏览器会话编排器 V0 起步：新增 `snapshot_browser_sessions` 后端命令，按账号批量返回运行状态、PID 和检查时间；前端账号页运行状态轮询改为消费轻量 session snapshot，保留旧窗口操作命令作为窗口按需读取能力。
- 浏览器会话编排 V0.2：检查窗口、前置窗口、平铺窗口、同步布局在执行前会先刷新一次轻量 session snapshot，再按最新运行账号集合继续操作；这样 Command+Q 退出、手动关闭窗口或刚启动后的状态漂移不再完全依赖上一轮 5 秒轮询。
- 浏览器会话编排 V0.3：`BrowserSessionSnapshot` 新增 `status: "running" | "stopped"`，前端运行判断改为读取 `status`；`running` 布尔值暂时保留为兼容/调试字段，后续可扩展 `starting`、`disconnected` 或带调试端口的完整会话模型。
- 浏览器会话编排 V0.4：`BrowserSessionSnapshot` 新增 `windowCount: number | null`。停止态固定为 `0`；轻量轮询中运行态为 `null`，表示没有触发窗口扫描、窗口数量未知；按需窗口扫描成功时填入实际窗口数，扫描失败时仍为 `null`。
- 浏览器会话编排 V0.5：前端新增 transient `starting` 状态。`open_profile` 命令成功后只把账号标记为“启动中”，不写入 `runningProfileIds`，因此不参与检查窗口、前置、平铺和同步布局；下一轮轻量 session snapshot 看到真实 Chrome 进程后才变为 `running` 并显示“运行中”。
- 浏览器会话编排 V0.6：启动命令成功后会安排一次 2 秒后的轻量 session snapshot 确认刷新；所有启动入口统一走 `launchChromeProfile`，账号卡、账号平台、批量打开、项目打开都会先登记“启动中”。如果短时间内连续启动多个账号，只保留一个确认刷新定时器，避免 50 个账号场景下堆叠大量后台检查。确认刷新失败或还没扫到进程时，不会清空已有运行状态，仍由原本 5 秒轮询继续纠偏。
- 浏览器会话编排 V0.7：会话快照刷新新增请求序号守卫。后台轮询、焦点刷新、启动确认刷新和窗口操作预刷新并发时，只有最新请求能写入 UI 状态，避免旧快照晚返回把“运行中”覆盖成“停止”；但窗口检查/平铺/同步这类用户动作仍可使用自己那次刷新返回的快照继续本次操作，不会因为后续后台刷新占用最新序号而误报没有运行账号。
- 浏览器会话编排 V0.8：账号页后台轻量轮询失败时不再清空 UI 会话状态，而是保留上一次快照，等下一次成功刷新再纠偏。手动窗口操作的预刷新失败行为保持不变，避免窗口操作在无法确认运行状态时继续误动作。
- 浏览器会话编排 V0.9：启动命令成功后生成的 transient `starting` 会话在短时间内受到保护。如果启动确认快照扫早并返回 `stopped`，不会立刻覆盖“启动中”；如果快照返回 `running`，仍会立即切换为“运行中”。`starting` 不写入运行账号集合，不参与检查窗口、前置、平铺或同步。
- 浏览器会话编排 V1.0：前端会话状态从 `App.tsx` 抽出。`src/browserSessions.ts` 负责纯会话模型和合并规则，`src/useBrowserSessions.ts` 负责 `sessionsById`、运行账号派生、请求序号、启动确认定时器和 `markStarting`。`App.tsx` 继续负责什么时候调用后端刷新、什么时候启动浏览器，UI 行为保持不变。
- 浏览器会话编排 V2.1：启动动作开始结构化。新增 `src/browserSessionLaunch.ts`，把启动成功/失败表示为 `BrowserLaunchResult`；`launchChromeProfile` 不再用 throw 作为常规失败控制流，而是返回结果。只有启动成功才标记 transient `starting` 并安排 2 秒确认刷新；普通启动、账号平台打开、项目打开和批量打开都读取 `result.ok` 决定是否计入成功。暂不引入通用 Operation 模型，避免和现有 `starting/running`、批量队列、窗口 busy 状态重叠。
- 浏览器会话编排 V2.2：启动结果增加账号级汇总。`src/browserSessionLaunch.ts` 新增 `summarizeBrowserLaunchResults` 和 `formatBrowserLaunchFailureDetails`；批量打开、项目打开会收集每个账号的启动结果，失败时提示前两个失败账号和原因，账号名缺失时回退到 profileId。项目多网址打开按账号算结果：任一网址失败则该账号整体不算成功，并展示首个失败原因。
- 浏览器会话编排 V2.3：新增最近启动事件。`src/browserSessionLaunch.ts` 新增 `BrowserLaunchEvent`、`browserLaunchEventFromResult` 和 `appendBrowserLaunchEvents`；账号卡、账号平台、批量打开、项目打开都会记录启动来源、目标网址、账号名、成功/失败和失败原因。账号页“更多操作”里显示最近 6 条，底层最多保留 30 条。
- 浏览器会话编排 V2.4：最近启动事件改为旁路持久化，保存到配置根目录 `app-data/launch-events.json`，启动时读取，新增启动记录后写回；不写入 `profiles.json`，避免污染账号/项目主索引。前端保存使用串行队列，Rust 写入使用临时文件加 rename，降低连续启动和半截 JSON 风险。
- 浏览器会话编排 V2.5：最近启动事件读取增加容错。`launch-events.json` 整体损坏时返回空记录；数组里混入坏 item 时跳过坏记录并保留合法记录，避免启动日志问题影响账号、项目和窗口操作主流程。原子写临时文件名加入进程内递增计数，避免同毫秒连续/并发保存时撞同一个 temp 文件。
- 浏览器会话编排 V2.6：批量/项目启动队列的收口逻辑抽为纯模型。`src/browserSessionLaunch.ts` 新增 `BrowserLaunchQueueSummary`、`summarizeBrowserLaunchQueue`、`formatBulkLaunchQueueMessage` 和 `formatProjectLaunchQueueMessage`，统一处理成功、部分失败、全部失败和用户停止的结束语义；`App.tsx` 的批量打开和项目打开改为消费该模型，文案保持现有行为。
- 浏览器会话编排 V2.7：新增 `selectRetryableBrowserLaunchProfileIds`，从启动结果中按原队列顺序挑出最新结果仍失败的账号，为后续“重试失败”入口准备数据边界。同一账号如果后续成功，会从重试候选里移除。
- 浏览器会话编排 V2.8：账号页批量栏新增“重试失败 N”入口。批量打开结束后，如果存在失败账号，会保存最近一次失败账号 ID 和本次打开网址；点击后只重试这些失败账号，并复用原批量打开流程、间隔、启动记录和最近网址逻辑。重试成功后清空入口，切换根目录时也会清空。
- 浏览器会话编排 V2.9：新一轮批量打开或重试真正开始前会清空上一轮 `lastBulkLaunchRetry`；如果本轮仍有失败，结束后再按本轮结果重新生成。这样旧“重试失败 N”按钮不会在新队列运行中继续显示。
- 浏览器会话编排 V2.10：批量网址输入框、常用/最近网址快捷填入、网址库填入都会清空旧 `lastBulkLaunchRetry`。这样修改目标网址后不会继续显示上一轮失败队列的“重试失败 N”。
- 浏览器会话编排 V3.0：新增 `src/browserOperations.ts`，定义通用 operation 模型和状态流转函数。当前只做纯模型，不复制 `BrowserLaunchResult`；批量启动类 operation 通过现有 `BrowserLaunchQueueSummary` 映射为 `succeeded/failed/cancelled`，为后续接入批量打开、项目打开、窗口同步和 AI 操作做统一生命周期边界。
- 浏览器会话编排 V3 路线：V3.1 把账号页“批量打开网址”登记为 operation；V3.2 把项目打开登记为 operation；V3.3 把检查/前置/平铺/同步布局登记为 operation；V3.4 再做正式的最近/当前 operation 展示；V3.5 处理同账号 active operation 冲突守卫。V4 才开始窗口注册表、布局模型、布局预设和同步控制器，不在 V3 里提前重做窗口同步。
- 浏览器会话编排 V3.1：账号页“批量打开网址”开始时会创建并启动一条 `bulk-open-url` operation，结束后复用 `BrowserLaunchQueueSummary` 标记为 `succeeded`、`failed` 或 `cancelled`；“更多操作”里新增轻量最近操作列表，显示类型、状态、目标网址和 `成功数 / 队列数`。该列表当前只保存在前端内存，不落盘，不作为运行状态来源。
- 浏览器会话编排 V3.2：项目页“开始打开”会创建并启动一条 `project-open` operation，目标记录项目 ID、项目名和本次打开的网址 ID；结束后继续复用 `BrowserLaunchQueueSummary` 标记为 `succeeded`、`failed` 或 `cancelled`。“更多操作”里能看到项目名、打开项目状态和 `成功数 / 队列数`。项目打开仍按原语义执行：同一账号依次打开项目的一组网址，再按项目间隔进入下一个账号；operation 仍只保存在前端内存，正式当前/历史操作面板留到 V3.4。
- 浏览器会话编排 V3.3：检查窗口、前置窗口、平铺窗口和同步布局开始执行后会创建 `window-action` operation，目标记录动作名；成功路径标记 `succeeded`，权限失败、窗口缺失、主窗口最小化、窗口移动未生效等明确失败或 early return 路径标记 `failed`。没有选中运行账号时不登记 operation，避免把未开始的动作写入历史。阶段测试先补“检查窗口完成后会登记窗口操作”和“同步布局失败后会登记窗口操作失败”两条失败测试，再实现统一窗口 operation helper；已执行 `npm test -- --run src/App.test.tsx -t "检查窗口完成后会登记窗口操作|同步布局失败后会登记窗口操作失败"` 和 `npm test`：通过。Claude CLI 本轮 45 秒无输出，未取得有效审查结果。
- 浏览器会话编排 V3.4：账号页“更多操作”里的 operation 面板升级为“当前操作”和“最近操作”两段。`queued/running` 归入当前操作，`succeeded/failed/cancelled` 归入最近操作；父组件不再先截断 operation 列表，避免历史记录较多时把正在执行的 operation 挤掉。最近操作仍只显示最近 6 条，operation 仍只保存在前端内存，不落盘、不新增取消按钮、不处理并发冲突。

## 验证记录

- `npm test`：通过。
- `npm run build`：通过。
- `CARGO_HOME=/Users/a0000/agent\ project/muti-chrome/.cargo cargo test`：通过。
- `CARGO_HOME=/Users/a0000/agent\ project/muti-chrome/.cargo npm run tauri:build`：通过。
- UI 第二版后重新执行 `npm test`、`npm run build`、`CARGO_HOME=/Users/a0000/agent\ project/muti-chrome/.cargo npm run tauri:build`：通过。
- Chrome profile 启动参数修复后重新执行 `npm test`、`npm run build`、`CARGO_HOME=/Users/a0000/agent\ project/muti-chrome/.cargo cargo test --manifest-path src-tauri/Cargo.toml`、`CARGO_HOME=/Users/a0000/agent\ project/muti-chrome/.cargo npm run tauri:build`：通过。
- 新建账号目录创建修复后重新执行 `npm test`、`npm run build`、`CARGO_HOME=/Users/a0000/agent\ project/muti-chrome/.cargo cargo test --manifest-path src-tauri/Cargo.toml`、`CARGO_HOME=/Users/a0000/agent\ project/muti-chrome/.cargo npm run tauri:build`：通过。
- V0.2 基础管理和图标替换后重新执行 `npm test`、`npm run build`、`CARGO_HOME=/Users/a0000/agent\ project/muti-chrome/.cargo cargo test --manifest-path src-tauri/Cargo.toml`、`CARGO_HOME=/Users/a0000/agent\ project/muti-chrome/.cargo npm run tauri:build`：通过。当前前端测试 4 个，Rust 测试 12 个。
- 删除确认面板修复后重新执行 `npm test`、`npm run build`、`CARGO_HOME=/Users/a0000/agent\ project/muti-chrome/.cargo cargo test --manifest-path src-tauri/Cargo.toml`、`CARGO_HOME=/Users/a0000/agent\ project/muti-chrome/.cargo npm run tauri:build`：通过。当前前端测试 5 个，Rust 测试 12 个。
- V0.3 启动器布局后重新执行 `npm test`、`npm run build`、`CARGO_HOME=/Users/a0000/agent\ project/muti-chrome/.cargo cargo test --manifest-path src-tauri/Cargo.toml`、`CARGO_HOME=/Users/a0000/agent\ project/muti-chrome/.cargo npm run tauri:build`：通过。当前前端测试 7 个，Rust 测试 12 个。
- V0.4 账号辨识和批量管理后重新执行 `npm test`、`npm run build`、`CARGO_HOME=/Users/a0000/agent\ project/muti-chrome/.cargo cargo test --manifest-path src-tauri/Cargo.toml`、`CARGO_HOME=/Users/a0000/agent\ project/muti-chrome/.cargo npm run tauri:build`：通过。当前前端测试 12 个，Rust 测试 12 个。
- 中等宽度工具栏和重复启动防护后，重新执行 `npm test`、`npm run build`、`CARGO_HOME=/Users/a0000/agent\ project/muti-chrome/.cargo cargo test --manifest-path src-tauri/Cargo.toml`、`CARGO_HOME=/Users/a0000/agent\ project/muti-chrome/.cargo npm run tauri:build`：通过。当前前端测试 15 个，Rust 测试 12 个。
- 外部启动器第一轮执行 `node --test experiments/launcher-apps/generate-launcher-apps.node-test.mjs`：通过，当时 5 个实验脚本测试；执行生成脚本后，`plutil -lint`、`file AppIcon.icns`、执行权限检查均通过。
- stay-open helper 修正为 AppleScript applet 后，`node --test experiments/launcher-apps/generate-launcher-apps.node-test.mjs`：通过，当前 9 个实验脚本测试。
- 目录健康检查完成后重新执行 `npm test`、`npm run build`、`CARGO_HOME=/Users/a0000/agent\ project/muti-chrome/.cargo cargo test --manifest-path src-tauri/Cargo.toml`、`node --test experiments/launcher-apps/generate-launcher-apps.node-test.mjs`：通过。当前前端测试 16 个，Rust 测试 15 个，实验脚本测试 9 个。
- 用户提醒后补充执行 `CARGO_HOME=/Users/a0000/agent\ project/muti-chrome/.cargo npm run tauri:build`：通过，已生成 `src-tauri/target/release/bundle/macos/MultiChrome.app`。
- 移除账号状态 UI 后重新执行 `npm test`、`npm run build`、`CARGO_HOME=/Users/a0000/agent\ project/muti-chrome/.cargo cargo test --manifest-path src-tauri/Cargo.toml`、`node --test experiments/launcher-apps/generate-launcher-apps.node-test.mjs`、`CARGO_HOME=/Users/a0000/agent\ project/muti-chrome/.cargo npm run tauri:build`：通过。已重新生成 `src-tauri/target/release/bundle/macos/MultiChrome.app`。
- 轻量备份/恢复完成后重新执行 `npm test`、`npm run build`、`CARGO_HOME=/Users/a0000/agent\ project/muti-chrome/.cargo cargo test --manifest-path src-tauri/Cargo.toml`、`node --test experiments/launcher-apps/generate-launcher-apps.node-test.mjs`、`CARGO_HOME=/Users/a0000/agent\ project/muti-chrome/.cargo npm run tauri:build`：通过。当前前端测试 17 个，Rust 测试 17 个，实验脚本测试 9 个；已重新生成 `src-tauri/target/release/bundle/macos/MultiChrome.app`。
- 打开数据目录/备份目录入口完成后重新执行 `npm test`、`npm run build`、`CARGO_HOME=/Users/a0000/agent\ project/muti-chrome/.cargo cargo test --manifest-path src-tauri/Cargo.toml`、`node --test experiments/launcher-apps/generate-launcher-apps.node-test.mjs`、`CARGO_HOME=/Users/a0000/agent\ project/muti-chrome/.cargo npm run tauri:build`：通过。当前前端测试 18 个，Rust 测试 18 个，实验脚本测试 9 个；已重新生成 `src-tauri/target/release/bundle/macos/MultiChrome.app`。
- 从备份恢复二次确认完成后重新执行 `npm test`、`npm run build`、`CARGO_HOME=/Users/a0000/agent\ project/muti-chrome/.cargo cargo test --manifest-path src-tauri/Cargo.toml`、`node --test experiments/launcher-apps/generate-launcher-apps.node-test.mjs`、`CARGO_HOME=/Users/a0000/agent\ project/muti-chrome/.cargo npm run tauri:build`：通过。当前前端测试 18 个，Rust 测试 18 个，实验脚本测试 9 个；已重新生成 `src-tauri/target/release/bundle/macos/MultiChrome.app`。
- 健康检查低风险修复完成后重新执行 `npm test`、`npm run build`、`CARGO_HOME=/Users/a0000/agent\ project/muti-chrome/.cargo cargo test --manifest-path src-tauri/Cargo.toml`、`node --test experiments/launcher-apps/generate-launcher-apps.node-test.mjs`、`CARGO_HOME=/Users/a0000/agent\ project/muti-chrome/.cargo npm run tauri:build`：通过。当前前端测试 19 个，Rust 测试 21 个，实验脚本测试 9 个；已重新生成 `src-tauri/target/release/bundle/macos/MultiChrome.app`。
- 孤儿 Profile 目录登记完成后重新执行 `npm test`、`npm run build`、`CARGO_HOME=/Users/a0000/agent\ project/muti-chrome/.cargo cargo test --manifest-path src-tauri/Cargo.toml`、`node --test experiments/launcher-apps/generate-launcher-apps.node-test.mjs`、`CARGO_HOME=/Users/a0000/agent\ project/muti-chrome/.cargo npm run tauri:build`：通过。当前前端测试 20 个，Rust 测试 21 个，实验脚本测试 9 个；已重新生成 `src-tauri/target/release/bundle/macos/MultiChrome.app`。
- 选中账号打开指定网址完成后重新执行 `npm test`、`npm run build`、`CARGO_HOME=/Users/a0000/agent\ project/muti-chrome/.cargo cargo test --manifest-path src-tauri/Cargo.toml`、`node --test experiments/launcher-apps/generate-launcher-apps.node-test.mjs`、`CARGO_HOME=/Users/a0000/agent\ project/muti-chrome/.cargo npm run tauri:build`：通过。当前前端测试 21 个，Rust 测试 22 个，实验脚本测试 9 个；已重新生成 `src-tauri/target/release/bundle/macos/MultiChrome.app`。
- 账号卡片重复点击误拦截修复使用 TDD：先添加“重复点击账号会再次请求打开同一 profile 的新标签”测试并确认失败，再移除长期已打开状态后通过。最终重新执行 `npm test`、`npm run build`、`CARGO_HOME=/Users/a0000/agent\ project/muti-chrome/.cargo cargo test --manifest-path src-tauri/Cargo.toml`、`node --test experiments/launcher-apps/generate-launcher-apps.node-test.mjs`、`CARGO_HOME=/Users/a0000/agent\ project/muti-chrome/.cargo npm run tauri:build`：通过。当前前端测试 21 个，Rust 测试 22 个，实验脚本测试 9 个；已重新生成 `src-tauri/target/release/bundle/macos/MultiChrome.app`。
- 批量打开队列使用 TDD：先添加“按自定义间隔逐个启动”和“可以中途停止”测试并确认失败，再实现队列、间隔输入和停止按钮后通过。最终重新执行 `npm test`、`npm run build`、`CARGO_HOME=/Users/a0000/agent\ project/muti-chrome/.cargo cargo test --manifest-path src-tauri/Cargo.toml`、`node --test experiments/launcher-apps/generate-launcher-apps.node-test.mjs`、`CARGO_HOME=/Users/a0000/agent\ project/muti-chrome/.cargo npm run tauri:build`：通过。当前前端测试 23 个，Rust 测试 22 个，实验脚本测试 9 个；已重新生成 `src-tauri/target/release/bundle/macos/MultiChrome.app`。
- 常用/最近网址完成后重新执行 `npm test`、`CARGO_HOME=/Users/a0000/agent\ project/muti-chrome/.cargo cargo test --manifest-path src-tauri/Cargo.toml`、`node --test experiments/launcher-apps/generate-launcher-apps.node-test.mjs`、`CARGO_HOME=/Users/a0000/agent\ project/muti-chrome/.cargo npm run tauri:build`：通过。当前前端测试 25 个，Rust 测试 22 个，实验脚本测试 9 个；已重新生成 `src-tauri/target/release/bundle/macos/MultiChrome.app`。
- 项目入口模块使用 TDD：先添加“可以在项目页新建项目并绑定账号”和“项目会按绑定账号和间隔打开网址”两个失败测试，再实现项目数据结构、左侧项目页、项目编辑弹窗和项目打开队列。最终重新执行 `npm test`、`npm run build`、`CARGO_HOME=/Users/a0000/agent\ project/muti-chrome/.cargo cargo test --manifest-path src-tauri/Cargo.toml`、`node --test experiments/launcher-apps/generate-launcher-apps.node-test.mjs`、`CARGO_HOME=/Users/a0000/agent\ project/muti-chrome/.cargo npm run tauri:build`：通过。当前前端测试 27 个，Rust 测试 22 个，实验脚本测试 9 个；已重新生成 `src-tauri/target/release/bundle/macos/MultiChrome.app`。
- 新建草稿交互使用 TDD：先添加“新建账号取消后不会留下账号”“新建账号保存后才创建账号”“新建项目取消后不会留下项目”等失败测试，再实现账号/项目草稿状态和保存按钮。最终重新执行 `npm test`、`npm run build`、`CARGO_HOME=/Users/a0000/agent\ project/muti-chrome/.cargo cargo test --manifest-path src-tauri/Cargo.toml`、`node --test experiments/launcher-apps/generate-launcher-apps.node-test.mjs`、`CARGO_HOME=/Users/a0000/agent\ project/muti-chrome/.cargo npm run tauri:build`：通过。当前前端测试 30 个，Rust 测试 22 个，实验脚本测试 9 个；已重新生成 `src-tauri/target/release/bundle/macos/MultiChrome.app`。
- 项目基础闭环使用 TDD：先添加项目搜索、复制、删除和停止队列四类失败测试，再实现项目查询、复制项目、删除确认和项目队列停止。最终重新执行 `npm test -- src/App.test.tsx -t "项目"`、`npm test`、`npm run build`、`CARGO_HOME=/Users/a0000/agent\ project/muti-chrome/.cargo cargo test --manifest-path src-tauri/Cargo.toml`、`node --test experiments/launcher-apps/generate-launcher-apps.node-test.mjs`、`CARGO_HOME=/Users/a0000/agent\ project/muti-chrome/.cargo npm run tauri:build`：通过。当前前端测试 34 个，Rust 测试 22 个，实验脚本测试 9 个；已重新生成 `src-tauri/target/release/bundle/macos/MultiChrome.app`。
- 账号平台资料库第一版使用 TDD：先添加新增平台资料、打开登录网址并复制用户名、删除平台资料三个失败测试，再实现账号资料结构、编辑弹窗 UI、Tauri 存储结构和打开/复制动作。最终重新执行 `npm test -- src/App.test.tsx -t "账号平台"`、`npm test`、`npm run build`、`CARGO_HOME=/Users/a0000/agent\ project/muti-chrome/.cargo cargo test --manifest-path src-tauri/Cargo.toml`、`node --test experiments/launcher-apps/generate-launcher-apps.node-test.mjs`、`CARGO_HOME=/Users/a0000/agent\ project/muti-chrome/.cargo npm run tauri:build`：通过。当前前端测试 37 个，Rust 测试 22 个，实验脚本测试 9 个；已重新生成 `src-tauri/target/release/bundle/macos/MultiChrome.app`。
- 账号编辑弹窗保存式草稿使用 TDD：先添加“保存前不写入、X 关闭丢弃、遮罩关闭丢弃、保存后写入”测试并确认失败，再实现编辑草稿、固定弹窗头尾和遮罩关闭。最终重新执行 `npm test -- src/App.test.tsx -t "编辑已有账号|账号平台"`、`npm test`、`CARGO_HOME=/Users/a0000/agent\ project/muti-chrome/.cargo npm run tauri:build`：通过。当前前端测试 40 个；已重新生成 `src-tauri/target/release/bundle/macos/MultiChrome.app`。
- 账号平台折叠行使用 TDD：先添加“已有账号平台默认折叠，点击编辑后展开字段”和“新增账号平台会直接展开方便录入”测试并确认失败，再实现平台行折叠/展开和精简样式。最终重新执行 `npm test -- src/App.test.tsx -t "账号平台"`、`npm test`、`npm run build`、`CARGO_HOME=/Users/a0000/agent\ project/muti-chrome/.cargo npm run tauri:build`：通过。当前前端测试 42 个；已重新生成 `src-tauri/target/release/bundle/macos/MultiChrome.app`。
- 项目编辑保存式草稿使用 TDD：先添加“关闭丢弃、遮罩关闭丢弃、保存才写入”三个失败测试，再实现项目编辑草稿和保存按钮。最终重新执行 `npm test -- src/App.test.tsx -t "编辑已有项目"`、`npm test -- src/App.test.tsx -t "项目"`、`npm test`、`npm run build`、`CARGO_HOME=/Users/a0000/agent\ project/muti-chrome/.cargo npm run tauri:build`：通过。当前前端测试 45 个；已重新生成 `src-tauri/target/release/bundle/macos/MultiChrome.app`。
- 项目卡片精简使用 TDD：先添加“项目卡片保持精简，低频资料留在编辑弹窗”和“可以在编辑弹窗复制项目”失败测试，再移除卡片备注/账号名/复制按钮并把复制入口移入项目编辑弹窗。最终重新执行 `npm test -- src/App.test.tsx -t "项目卡片|复制项目"`、`npm test -- src/App.test.tsx -t "项目"`、`npm test`、`npm run build`、`CARGO_HOME=/Users/a0000/agent\ project/muti-chrome/.cargo npm run tauri:build`：通过。当前前端测试 46 个；已重新生成 `src-tauri/target/release/bundle/macos/MultiChrome.app`。
- 项目编辑弹窗布局修复使用 TDD：先添加“项目编辑弹窗使用统一滚动内容和固定保存栏”失败测试，确认旧结构直接把字段、保存栏和危险区平铺为子级；修复后重新执行该测试、`npm test -- src/App.test.tsx -t "项目"`、`npm test`、`npm run build`、Playwright 本地截图检查、`CARGO_HOME=/Users/a0000/agent\ project/muti-chrome/.cargo npm run tauri:build`：通过。当前前端测试 47 个；已重新生成 `src-tauri/target/release/bundle/macos/MultiChrome.app`。
- 编辑弹窗遮罩关闭一致性修复使用 TDD：先补强账号和项目遮罩关闭测试，确认账号弹窗点击遮罩未关闭；修复后重新执行 `npm test -- src/App.test.tsx -t "遮罩关闭"`、`npm test`、`npm run build`、`CARGO_HOME=/Users/a0000/agent\ project/muti-chrome/.cargo npm run tauri:build`：通过。当前前端测试 47 个；已重新生成 `src-tauri/target/release/bundle/macos/MultiChrome.app`。
- 项目多网址使用 TDD：先添加“保存多个网址”“打开全部网址按账号队列执行”“只打开选中的单个网址”三个失败测试，再实现 `urls` 数据结构、编辑弹窗网址列表、项目卡片打开范围选择和多网址队列。最终重新执行 `npm test -- src/App.test.tsx -t "项目"`、`npm test`、`npm run build`、`CARGO_HOME=/Users/a0000/agent\ project/muti-chrome/.cargo cargo test --manifest-path src-tauri/Cargo.toml`、`node --test experiments/launcher-apps/generate-launcher-apps.node-test.mjs`、`CARGO_HOME=/Users/a0000/agent\ project/muti-chrome/.cargo npm run tauri:build`：通过。当前前端测试 50 个，Rust 测试 22 个，实验脚本测试 9 个；已重新生成 `src-tauri/target/release/bundle/macos/MultiChrome.app`。
- 项目网址管理增强使用 TDD：先添加“批量导入多行网址”“项目网址上移/下移排序”“复制单条网址”三个失败测试，再实现导入解析器、网址操作按钮和复制反馈。最终重新执行 `npm test -- src/App.test.tsx -t "项目"`、`npm test`、`npm run build`、`CARGO_HOME=/Users/a0000/agent\ project/muti-chrome/.cargo cargo test --manifest-path src-tauri/Cargo.toml`、`node --test experiments/launcher-apps/generate-launcher-apps.node-test.mjs`、`CARGO_HOME=/Users/a0000/agent\ project/muti-chrome/.cargo npm run tauri:build`：通过。当前前端测试 53 个，Rust 测试 22 个，实验脚本测试 9 个；已重新生成 `src-tauri/target/release/bundle/macos/MultiChrome.app`。
- 账号批量新建使用 TDD：先添加“批量新建账号取消不落盘”和“批量新建账号并解析标签/备注”两个失败测试，再实现批量新建弹窗、文本解析和预览。最终重新执行 `npm test -- src/App.test.tsx -t "新建账号|批量新建账号|编辑已有账号"`、`npm test`、`npm run build`、`CARGO_HOME=/Users/a0000/agent\ project/muti-chrome/.cargo cargo test --manifest-path src-tauri/Cargo.toml`、`node --test experiments/launcher-apps/generate-launcher-apps.node-test.mjs`、`CARGO_HOME=/Users/a0000/agent\ project/muti-chrome/.cargo npm run tauri:build`：通过。当前前端测试 55 个，Rust 测试 22 个，实验脚本测试 9 个；已重新生成 `src-tauri/target/release/bundle/macos/MultiChrome.app`。
- 批量新建账号表格粘贴优化使用 TDD：先添加“批量新建账号支持从表格粘贴制表符分隔内容”测试并确认失败，再让解析器优先识别 Tab 分隔。最终重新执行 `npm test -- src/App.test.tsx -t 制表符`、`npm test`、`npm run build`：通过。当前前端测试 56 个。
- 批量导入已有 profile 使用 TDD：先添加扫描预览、批量导入成功、导入失败回滚三条前端失败测试，以及旧 MultiChrome 索引识别、一层扫描/可疑判断两条 Rust 失败测试；再实现 `scan_profile_import_candidates`、前端导入候选 UI、确认导入和失败回滚。阶段验证执行 `npm test -- src/App.test.tsx -t "导入"`、`npm test`、`npm run build`、`CARGO_HOME=/Users/a0000/agent\ project/muti-chrome/.cargo cargo test --manifest-path src-tauri/Cargo.toml`：通过。当前前端测试 59 个，Rust 测试 24 个。
- 批量导入去重使用 TDD：先添加“重复候选禁用”和“导入时写 marker/importSource”前端失败测试，以及“写 `.multichrome.json`”和“来源路径重复识别”Rust 失败测试；再实现 `ProfileMarker`、`importSource`、marker 读写和扫描去重。阶段验证执行 `npm test -- src/App.test.tsx -t "导入"`、`npm test`、`npm run build`、`CARGO_HOME=/Users/a0000/agent\ project/muti-chrome/.cargo cargo test --manifest-path src-tauri/Cargo.toml`：通过。当前前端测试 60 个，Rust 测试 26 个。
- 使用 `open -n .launcher-experiment/MC-account-001.app` 和 `open -n .launcher-experiment/MC-account-002.app` 做桌面验证；进程参数确认分别启动 `/Users/a0000/MultiChromeProfiles/profiles/account-001` 和 `/Users/a0000/MultiChromeProfiles/profiles/account-002`，且没有传 `about:blank`。
- 使用 `open -n .launcher-helper-experiment/MC-account-001.app` 和 `open -n .launcher-helper-experiment/MC-account-002.app` 做桌面验证；进程参数确认两个 AppleScript `applet` helper 分别保持运行，且两个 Chrome 主进程分别使用 `account-001`、`account-002` 的 `--user-data-dir`。
- 使用不带 `-n` 的 `open .launcher-helper-experiment/MC-account-001.app` 模拟 reopen；验证后 `account-001` helper 仍只有一个 applet 进程。
- 使用内置浏览器检查 `http://127.0.0.1:5173/`：主界面无右侧 Inspector，账号卡片宽度稳定为 354px；720px 高度下编辑弹窗可见底部危险区；控制台无 error。
- 桌面人工测试使用 `/private/tmp/MultiChromeProfiles-Codex-Test`：新建、编辑、归档/恢复、复制、复制导入、刷新大小、浏览器路径检测、打开 Chrome、打开文件夹、只删记录、删除文件夹均通过。
- 已打开 bundle 配置并生成 macOS `.app`：`src-tauri/target/release/bundle/macos/MultiChrome.app`。
- 白天/夜晚主题使用 TDD：先添加“设置弹窗的主题草稿关闭会丢弃，保存后才持久化”失败测试，再实现 `settings.theme`、设置内主题切换、关闭回滚、保存持久化和暗色 CSS 覆盖。最终重新执行 `npm test -- src/App.test.tsx -t "主题草稿"`、`npm test -- src/App.test.tsx -t "设置弹窗"`、`npm test`、`cargo test`、`npm run build`、`npm run tauri:build`：通过。当前前端测试 69 个，Rust 测试 26 个；已重新生成 `src-tauri/target/release/bundle/macos/MultiChrome.app`。
- 完整 profile 备份/恢复使用 TDD：先添加 Rust 完整备份/恢复核心测试和前端设置弹窗交互测试并确认失败，再实现 Rust 备份目录、manifest、扫描预览、合并恢复和前端设置入口/确认弹窗。最终重新执行 `npm test -- src/App.test.tsx -t "完整备份"`、`npm test`、`cargo test`、`npm run build`、`npm run tauri:build`：通过。当前前端测试 71 个，Rust 测试 30 个；已重新生成 `src-tauri/target/release/bundle/macos/MultiChrome.app`。
- 批量删除账号使用 TDD：先添加“批量删除只删除记录”和“删除文件夹失败不移除账号索引”两个失败测试，再实现批量栏入口、独立确认弹窗和删除处理。最终重新执行 `npm test -- src/App.test.tsx -t "批量删除"`、`npm test`、`npm run build`、`cargo test`、`npm run tauri:build`：通过。当前前端测试 73 个，Rust 测试 30 个；已重新生成 `src-tauri/target/release/bundle/macos/MultiChrome.app`。
- 账号平台常用模板使用 TDD：先添加“套用 Galxe 模板且保存前不落盘”失败测试，再实现模板按钮和明暗主题样式。最终重新执行 `npm test -- src/App.test.tsx -t "账号平台可以套用常用模板"`、`npm test -- src/App.test.tsx -t "账号平台"`、`npm test`、`npm run build`、`cargo test --manifest-path src-tauri/Cargo.toml`、`node --test experiments/launcher-apps/generate-launcher-apps.node-test.mjs`、`npm run tauri:build`：通过。当前前端测试 74 个，Rust 测试 30 个，实验脚本测试 9 个；已重新生成 `src-tauri/target/release/bundle/macos/MultiChrome.app`。
- 运行状态识别使用 TDD：先添加“账号卡片显示正在运行的 Chrome profile”和“刷新大小时会同步刷新账号运行状态”前端失败测试，再添加 Rust 进程解析失败测试，随后实现 `list_running_profiles` 命令、前端 API、临时运行状态和卡片徽标。最终重新执行 `npm test`、`cargo test --manifest-path src-tauri/Cargo.toml`、`npm run build`、`node --test experiments/launcher-apps/generate-launcher-apps.node-test.mjs`、`npm run tauri:build`：通过。当前前端测试 76 个，Rust 测试 31 个，实验脚本测试 9 个；已重新生成 `src-tauri/target/release/bundle/macos/MultiChrome.app`。
- 切换到运行中账号使用 TDD：先添加“运行中的账号可以切换到对应 Chrome 进程”前端失败测试，再添加 Rust PID 解析测试，随后实现 `focus_profile_window` 命令、前端 API 和卡片切换按钮。最终重新执行 `npm test`、`cargo test --manifest-path src-tauri/Cargo.toml`、`npm run build`、`node --test experiments/launcher-apps/generate-launcher-apps.node-test.mjs`、`npm run tauri:build`：通过。当前前端测试 77 个，Rust 测试 32 个，实验脚本测试 9 个；已重新生成 `src-tauri/target/release/bundle/macos/MultiChrome.app`。
- 选中账号检查窗口使用 TDD：先添加“批量栏可以检查选中运行账号的窗口数量”前端失败测试，再添加 Rust AppleScript 输出解析失败测试，随后实现 `list_profile_windows`、前端 API、批量栏按钮和窗口数量汇总消息。最终重新执行 `npm test`、`cargo test --manifest-path src-tauri/Cargo.toml`、`npm run build`、`node --test experiments/launcher-apps/generate-launcher-apps.node-test.mjs`、`npm run tauri:build`：通过。当前前端测试 78 个，Rust 测试 33 个，实验脚本测试 9 个；已重新生成 `src-tauri/target/release/bundle/macos/MultiChrome.app`。
- 窗口位置/尺寸读取使用 TDD：先把窗口检查测试升级为要求显示 `宽x高 @ x,y`，再升级 Rust 解析测试要求解析坐标和尺寸；实现 `ChromeWindowInfo` 的 `x/y/width/height` 字段、AppleScript `position/size` 读取和前端摘要格式。最终重新执行 `npm test`、`cargo test --manifest-path src-tauri/Cargo.toml`、`npm run build`、`node --test experiments/launcher-apps/generate-launcher-apps.node-test.mjs`、`npm run tauri:build`：通过。当前前端测试 78 个，Rust 测试 33 个，实验脚本测试 9 个；已重新生成 `src-tauri/target/release/bundle/macos/MultiChrome.app`。
- 平铺窗口小实验使用 TDD：先添加“批量栏可以平铺选中运行账号的首个窗口”前端失败测试，再添加 Rust `set_window_bounds_script` 失败测试，随后实现 `set_profile_window_bounds` 后端命令、前端 API、网格布局计算和批量栏“平铺窗口”按钮。最终重新执行 `npm test`、`cargo test --manifest-path src-tauri/Cargo.toml`、`npm run build`、`node --test experiments/launcher-apps/generate-launcher-apps.node-test.mjs`、`npm run tauri:build`：通过。当前前端测试 79 个，Rust 测试 34 个，实验脚本测试 9 个；已重新生成 `src-tauri/target/release/bundle/macos/MultiChrome.app`。
- 平铺窗口预检查使用 TDD：先添加“平铺窗口会跳过没有可读窗口的运行账号”失败测试，确认旧实现不会调用窗口检查；随后让平铺流程先读取窗口列表、跳过无窗口账号，并按实际可平铺数量重新计算布局。最终重新执行 `npm test -- src/App.test.tsx -t "平铺"`、`npm test`、`cargo test --manifest-path src-tauri/Cargo.toml`、`node --test experiments/launcher-apps/generate-launcher-apps.node-test.mjs`、`npm run build`、`npm run tauri:build`：通过。当前前端测试 80 个，Rust 测试 34 个，实验脚本测试 9 个；已重新生成 `src-tauri/target/release/bundle/macos/MultiChrome.app`。
- 窗口操作权限失败提示使用 TDD：先添加“切换窗口失败时会提示 macOS 辅助功能权限”和“平铺窗口检查全部失败时会提示 macOS 辅助功能权限”两个失败测试，再实现统一 `windowAutomationErrorMessage`，用于窗口切换、窗口检查和平铺失败。最终重新执行 `npm test -- src/App.test.tsx -t "辅助功能权限"`、`npm test`、`cargo test --manifest-path src-tauri/Cargo.toml`、`node --test experiments/launcher-apps/generate-launcher-apps.node-test.mjs`、`npm run build`、`npm run tauri:build`：通过。当前前端测试 82 个，Rust 测试 34 个，实验脚本测试 9 个；已重新生成 `src-tauri/target/release/bundle/macos/MultiChrome.app`。
- 平铺窗口可用区域偏移使用 TDD：先添加“平铺窗口会使用屏幕可用区域的左上角偏移”失败测试，确认旧实现仍从 `0,0` 开始；随后让 `tileBoundsForCount` 支持原点偏移，并通过兼容类型读取 `screen.availLeft/availTop`。最终重新执行 `npm test -- src/App.test.tsx -t "屏幕可用区域"`、`npm test`、`cargo test --manifest-path src-tauri/Cargo.toml`、`node --test experiments/launcher-apps/generate-launcher-apps.node-test.mjs`、`npm run build`、`npm run tauri:build`：通过。当前前端测试 83 个，Rust 测试 34 个，实验脚本测试 9 个；已重新生成 `src-tauri/target/release/bundle/macos/MultiChrome.app`。
- 平铺窗口数量保护使用 TDD：先添加“平铺窗口过多时会提示分批处理”失败测试，确认 1200x800 屏幕上 10 个窗口会被压成 `300x266` 并直接移动；随后增加 `320x240` 最小可用尺寸估算和容量提示，超过容量时不移动窗口。最终重新执行 `npm test -- src/App.test.tsx -t "平铺窗口过多"`、`npm test`、`cargo test --manifest-path src-tauri/Cargo.toml`、`node --test experiments/launcher-apps/generate-launcher-apps.node-test.mjs`、`npm run build`、`npm run tauri:build`：通过。当前前端测试 84 个，Rust 测试 34 个，实验脚本测试 9 个；已重新生成 `src-tauri/target/release/bundle/macos/MultiChrome.app`。
- 多窗口账号平铺提示使用 TDD：先添加“平铺窗口会提示多窗口账号只处理首个窗口”失败测试，确认原行为只显示“已平铺 2 个窗口”；随后在平铺预检查阶段统计 `windows.length > 1` 的账号数量，并把提示追加到完成消息。最终重新执行 `npm test -- src/App.test.tsx -t "平铺窗口会提示多窗口账号只处理首个窗口"`、`npm test`、`cargo test --manifest-path src-tauri/Cargo.toml`、`node --test experiments/launcher-apps/generate-launcher-apps.node-test.mjs`、`npm run build`、`npm run tauri:build`：通过。当前前端测试 85 个，Rust 测试 34 个，实验脚本测试 9 个；已重新生成 `src-tauri/target/release/bundle/macos/MultiChrome.app`。
- 最小化状态展示使用 TDD：先添加前端“窗口检查会提示首个窗口已最小化”和后端 `chrome_windows_from_osascript_output_parses_minimized_state` 失败测试；随后在 AppleScript 输出中读取 `AXMinimized`，解析器兼容旧 6 列输出并默认非最小化，前端摘要只在 `minimized` 为 true 时追加“已最小化”。最终重新执行 `npm test -- src/App.test.tsx -t "窗口检查会提示首个窗口已最小化"`、`cargo test --manifest-path src-tauri/Cargo.toml chrome_windows_from_osascript_output_parses_minimized_state`、`npm test`、`cargo test --manifest-path src-tauri/Cargo.toml`、`node --test experiments/launcher-apps/generate-launcher-apps.node-test.mjs`、`npm run build`、`npm run tauri:build`：通过。当前前端测试 86 个，Rust 测试 35 个，实验脚本测试 9 个；已重新生成 `src-tauri/target/release/bundle/macos/MultiChrome.app`。
- 布局同步 V0 使用 TDD：先添加“批量栏可以把主账号首个窗口布局同步给其它运行账号”失败测试，确认原批量栏没有“同步布局”入口；随后新增 `windowSyncing` 状态、主账号选择、`syncLayoutForSelectedProfiles` 流程和紧凑工具栏样式。实现采用“先读取主账号首窗 bounds 快照，再逐个目标账号检查窗口并设置 bounds”的保守策略，目标窗口关闭或不可读时计入提示，不中断其它账号。最终重新执行 `npm test -- src/App.test.tsx -t "批量栏可以把主账号首个窗口布局同步给其它运行账号"`、`npm test`、`cargo test --manifest-path src-tauri/Cargo.toml`、`node --test experiments/launcher-apps/generate-launcher-apps.node-test.mjs`、`npm run build`、`npm run tauri:build`：通过。当前前端测试 87 个，Rust 测试 35 个，实验脚本测试 9 个；已重新生成 `src-tauri/target/release/bundle/macos/MultiChrome.app`。
- 布局同步最小化保护使用 TDD：先添加“布局同步会拒绝使用已最小化的主账号窗口”和“布局同步会跳过已最小化的目标窗口”两个失败测试，确认旧实现会继续读取目标并设置最小化窗口 bounds；随后在同步流程中检查 `ChromeWindowInfo.minimized`，主窗口最小化时直接中止，目标窗口最小化时跳过并汇总提示。最终重新执行 `npm test -- src/App.test.tsx -t "布局同步会"`、`npm test -- src/App.test.tsx -t "布局同步"`、`npm test`、`cargo test --manifest-path src-tauri/Cargo.toml`、`node --test experiments/launcher-apps/generate-launcher-apps.node-test.mjs`、`npm run build`、`npm run tauri:build`：通过。当前前端测试 89 个，Rust 测试 35 个，实验脚本测试 9 个；已重新生成 `src-tauri/target/release/bundle/macos/MultiChrome.app`。
- 窗口移动读回验证使用 TDD：先添加“布局同步会在窗口没有实际移动时提示未生效”和“平铺窗口会在窗口没有实际移动时提示未生效”两个失败测试，确认旧实现只要 AppleScript 设置命令无错误就提示成功；随后让同步布局和平铺窗口都在设置 bounds 后重新读取目标账号首个窗口，并用 8px 容差校验位置/尺寸，未匹配时计入“未生效”。阶段验证执行 `npm test -- src/App.test.tsx -t "布局同步"`、`npm test -- src/App.test.tsx -t "平铺窗口"`、`npm test`、`cargo test --manifest-path src-tauri/Cargo.toml`、`node --test experiments/launcher-apps/generate-launcher-apps.node-test.mjs`、`npm run build`、`npm run tauri:build`：通过。当前前端测试 91 个，Rust 测试 35 个，实验脚本测试 9 个；已重新生成 `src-tauri/target/release/bundle/macos/MultiChrome.app`。
- osascript 权限提示修复使用 TDD：用临时 Chrome profile 在本机复现 `“osascript”不允许辅助访问。(-25211)`，确认窗口读取链路被 `/usr/bin/osascript` 辅助功能权限拦截；先添加“osascript 辅助功能失败时会提示允许 osascript”失败测试，再细分错误文案，提示用户同时允许 MultiChrome 和 `/usr/bin/osascript`。最终重新执行 `npm test -- src/App.test.tsx -t "osascript 辅助功能失败时会提示允许 osascript|辅助功能权限|切换窗口失败"`、`npm test`、`cargo test --manifest-path src-tauri/Cargo.toml`、`node --test experiments/launcher-apps/generate-launcher-apps.node-test.mjs`、`npm run build`、`npm run tauri:build`：通过。当前前端测试 92 个，Rust 测试 35 个，实验脚本测试 9 个；已重新生成 `src-tauri/target/release/bundle/macos/MultiChrome.app`。
- 窗口控制底层重构：`focus_profile_window`、`list_profile_windows`、`set_profile_window_bounds` 不再通过 `/usr/bin/osascript` 执行 AppleScript，而是由 Rust 直接调用 macOS Accessibility API，按 Chrome profile 主进程 PID 读取 `AXWindows`、`AXPosition`、`AXSize`、`AXMinimized`，并通过 `AXRaise` 拉前台。仍需要在 macOS 辅助功能里授权 MultiChrome，但不再要求额外授权 `/usr/bin/osascript`。
- 平铺/同步成功后新增前台切换：平铺窗口成功后会切换到首个成功平铺账号；布局同步成功后会切换到主账号。若窗口移动成功但前台切换失败，会在成功消息后追加提示，不把已完成的移动误判为失败。
- 打包流程新增固定签名脚本：`npm run tauri:build` 会在 Tauri 生成 `.app` 后执行 `scripts/sign-macos-app.mjs`，用固定 `app.multichrome.desktop` 做本地 ad-hoc 签名，减少开发期反复打包导致辅助功能授权漂移的问题。
- 用户重新打开并授权当前签名后的 MultiChrome 后，实测确认两个 Chrome profile 窗口可以正常平铺，并按比例分开。
- 重复点击已运行账号启动修复后，新增 Rust 单测覆盖未运行/已运行两条启动命令路径，并给 `.app` 包内可执行文件名不等于 app 名的情况补了 fallback 测试。最终重新执行 `npm test`、`cargo test --manifest-path src-tauri/Cargo.toml`、`node --test experiments/launcher-apps/generate-launcher-apps.node-test.mjs`、`npm run build`、`npm run tauri:build`：通过。真实进程验证中，对正在运行的 `account-002` 直接投递同一 `--user-data-dir` 后，Chrome 返回“正在现有的浏览器会话中打开。”，且 `ps` 没有新增同账号主进程。
- 账号卡片右侧布局修复后，用 Playwright 在真实浏览器中验证暗色模式下未运行态和运行态的按钮/状态 bbox 均不相交；随后重新执行 `npm test`、`cargo test --manifest-path src-tauri/Cargo.toml`、`npm run build`、`npm run tauri:build`：通过，已重新生成 macOS `.app`。
- 运行状态动态刷新和账号卡片大小精简使用 TDD：先添加“运行状态会轻量动态刷新并移除已退出账号”和“账号卡片不显示目录大小，编辑时只刷新当前账号大小”两个失败测试，再实现账号页 5 秒运行状态轮询、前台/可见刷新、移除卡片大小和“刷新大小”按钮。阶段验证执行 `npm test -- src/App.test.tsx -t "运行状态会轻量动态刷新|账号卡片不显示目录大小"`、`npm test`、`npm run build`、`npm run tauri:build`：通过。当前前端测试 93 个，已重新生成 macOS `.app`。
- 网址库表格版使用 TDD：左侧“网址库”入口改为 Notion database 风格表格，列为名称、URL、标签、备注和操作；新建/编辑改为独立弹窗草稿，点 X、取消或遮罩关闭不写入，点“保存网址”才持久化；旧常用网址会自动迁移为网址库条目；可从网址库回填账号页的批量打开输入框，也可直接用当前选中账号打开某条网址；删除网址使用独立确认弹窗。阶段验证执行 `npm test -- src/App.test.tsx -t "网址库"`、`npm test`、`cargo test --manifest-path src-tauri/Cargo.toml`、`npm run build` 和 Playwright 本地 UI smoke：通过。当前前端测试 97 个，Rust 测试 35 个。
- 批量前置窗口使用 TDD：先把“平铺窗口”测试改为要求每个成功平铺窗口都执行前置，并新增“批量栏可以前置选中运行账号窗口”失败测试；随后在“更多操作”中加入“前置窗口”按钮，平铺成功后自动前置所有成功平铺窗口，布局同步成功后先前置目标账号、最后前置主账号。阶段验证执行 `npm test -- src/App.test.tsx -t "平铺选中运行账号|前置选中运行账号"`、`npm test`、`npm run build`、`cargo test`、`npm run tauri:build`：通过。当前前端测试 104 个，Rust 测试 36 个；已重新生成 macOS `.app`。实机验证时发现重新打包后 macOS 可能重置辅助功能授权，需要用户重新授权当前 MultiChrome 后再测真实前置效果。
- 批量打开空网址使用 TDD：先添加“批量打开网址为空时会打开默认新标签页”失败测试，再让空输入走 `chrome://newtab/`，且不把默认新标签页写入最近网址。阶段验证执行 `npm test -- src/App.test.tsx -t "批量打开网址为空时会打开默认新标签页"`、`npm test`、`npm run build`：通过。当前前端测试 105 个。
- 浏览器会话编排 V0.1 使用 TDD：先添加“账号页运行状态轮询显式请求轻量快照”和“轻量快照不触发窗口扫描”两个失败测试，再给 `snapshot_browser_sessions` 增加 `includeWindows` 开关。账号页 5 秒轮询现在只扫描进程状态和 PID，不读取窗口列表；窗口检查、平铺、同步布局仍按需读取窗口。阶段验证执行 `npm test -- src/App.test.tsx -t "账号卡片运行状态来自浏览器会话快照"`、`cargo test --manifest-path src-tauri/Cargo.toml browser_session_snapshots_skip_window_scan_in_lightweight_mode`、`npm test`、`cargo test --manifest-path src-tauri/Cargo.toml`、`npm run build`：通过。当前前端测试 105 个，Rust 测试 37 个。
- 浏览器会话编排 V0.2 使用 TDD：先添加“窗口操作前会刷新轻量会话状态”失败测试，再让检查窗口、前置窗口、平铺窗口和同步布局统一在执行前刷新轻量 session snapshot，并按最新 running 集合过滤选中账号。阶段验证执行 `npm test -- src/App.test.tsx -t "窗口操作前会刷新轻量会话状态"`、`npm test -- src/App.test.tsx -t "检查窗口|前置选中运行账号|平铺窗口|布局同步|窗口操作前会刷新轻量会话状态"`、`npm test`、`cargo test --manifest-path src-tauri/Cargo.toml`、`npm run build`、`npm run tauri:build`、`codesign --verify --deep --strict --verbose=2 src-tauri/target/release/bundle/macos/MultiChrome.app`、`git diff --check`：通过。当前前端测试 106 个，Rust 测试 37 个。
- 浏览器会话编排 V0.3 使用 TDD：先让 Rust session snapshot 测试期望 `BrowserSessionStatus` 并确认编译失败，再新增 `BrowserSessionStatus`、序列化 `status` 字段、前端 `BrowserSessionStatus` 类型和基于 `status` 的运行态判断；浏览器测试环境 fallback 也返回同一字段。阶段验证执行 `cargo test --manifest-path src-tauri/Cargo.toml browser_session_snapshots -- --nocapture`、`npm test -- src/App.test.tsx -t "账号卡片运行状态来自浏览器会话快照|窗口操作前会刷新轻量会话状态|运行状态会轻量动态刷新"`、`npm test`、`cargo test --manifest-path src-tauri/Cargo.toml`、`npm run build`、`npm run tauri:build`、`codesign --verify --deep --strict --verbose=2 src-tauri/target/release/bundle/macos/MultiChrome.app`、`git diff --check`：通过。当前前端测试 106 个，Rust 测试 37 个。
- 浏览器会话编排 V0.4 使用 TDD：先让 Rust session snapshot 测试期望 `window_count` 并确认编译失败，再新增 `windowCount` 序列化字段和前端类型。阶段验证执行 `cargo test --manifest-path src-tauri/Cargo.toml browser_session_snapshots -- --nocapture`、`npm test -- src/App.test.tsx -t "账号卡片运行状态来自浏览器会话快照|窗口操作前会刷新轻量会话状态|运行状态会轻量动态刷新"`、`npm run build`、`npm test`、`cargo test --manifest-path src-tauri/Cargo.toml`、`npm run tauri:build`、`codesign --verify --deep --strict --verbose=2 src-tauri/target/release/bundle/macos/MultiChrome.app`、`git diff --check`：通过。当前前端测试 106 个，Rust 测试 38 个。
- 浏览器会话编排 V0.5 使用 TDD：先添加“启动命令成功后先标记为启动中而不是运行中”失败测试，再实现前端 `starting` 临时态和账号卡片“启动中”徽标。阶段验证执行 `npm test -- src/App.test.tsx -t "启动命令成功后先标记为启动中而不是运行中"`、`npm test -- src/App.test.tsx -t "启动命令成功后先标记为启动中|账号卡片运行状态来自浏览器会话快照|运行状态会轻量动态刷新|运行中的账号可以切换|窗口操作前会刷新轻量会话状态|检查窗口|前置选中运行账号|平铺窗口|布局同步"`、`npm test`、`cargo test --manifest-path src-tauri/Cargo.toml`、`npm run build`、`npm run tauri:build`、`codesign --verify --deep --strict --verbose=2 src-tauri/target/release/bundle/macos/MultiChrome.app`、`git diff --check`：通过。当前前端测试 107 个，Rust 测试 38 个，已重新生成 macOS `.app`。
- 浏览器会话编排 V0.6 使用 TDD：先添加“启动后会短延迟刷新会话并确认运行状态”失败测试，确认旧实现会一直停在“启动中”；随后新增启动确认定时器，在启动成功后 2 秒触发一次轻量 session snapshot。Claude 红队指出批量打开、项目打开绕过启动临时态，以及确认刷新失败可能清空已有运行状态；已补失败测试并修复。阶段验证执行 `npm test -- src/App.test.tsx -t "批量打开会标记启动中|项目打开会标记启动中|启动确认刷新失败不会清空已有运行状态"`、`npm test -- src/App.test.tsx -t "启动命令成功后先标记为启动中|启动后会短延迟刷新会话|启动确认刷新失败不会清空已有运行状态|批量打开会标记启动中|项目打开会标记启动中|账号卡片运行状态来自浏览器会话快照|运行状态会轻量动态刷新|窗口操作前会刷新轻量会话状态|检查窗口|前置选中运行账号|平铺窗口|布局同步|可以给选中的账号批量打开指定网址|项目会按绑定账号和间隔打开网址"`、`npm test`、`cargo test --manifest-path src-tauri/Cargo.toml`、`npm run build`、`npm run tauri:build`、`codesign --verify --deep --strict --verbose=2 src-tauri/target/release/bundle/macos/MultiChrome.app`、`git diff --check`：通过。当前前端测试 111 个，Rust 测试 38 个，已重新生成 macOS `.app`。
- 浏览器会话编排 V0.7 使用 TDD：先添加“较旧的会话快照晚返回时不会覆盖较新的运行状态”失败测试，确认旧请求会覆盖新 UI；随后新增请求序号守卫。再补“窗口操作会使用自身刷新结果，即使后续后台快照占用最新序号”失败测试，确认不能简单把 stale 结果返回空数组；最终调整为 stale 请求不写 UI、但仍返回给自己的调用链。阶段验证执行 `npm test -- src/App.test.tsx -t "较旧的会话快照晚返回|窗口操作会使用自身刷新结果"`、`npm test -- src/App.test.tsx -t "较旧的会话快照晚返回|窗口操作会使用自身刷新结果|启动命令成功后先标记为启动中|启动后会短延迟刷新会话|启动确认刷新失败不会清空已有运行状态|批量打开会标记启动中|项目打开会标记启动中|账号卡片运行状态来自浏览器会话快照|运行状态会轻量动态刷新|窗口操作前会刷新轻量会话状态|检查窗口|前置选中运行账号|平铺窗口|布局同步"`、`npm test`、`cargo test --manifest-path src-tauri/Cargo.toml`、`npm run build`、`npm run tauri:build`、`codesign --verify --deep --strict --verbose=2 src-tauri/target/release/bundle/macos/MultiChrome.app`、`git diff --check`：通过。当前前端测试 113 个，Rust 测试 38 个，已重新生成 macOS `.app`。
- 浏览器会话编排 V0.8 使用 TDD：先添加“运行状态轻量刷新失败时保留上一次会话状态”失败测试，确认第二轮轻量快照失败会清空“运行中”；随后让账号页后台轮询失败时保留上一轮状态。阶段验证执行 `npm test -- src/App.test.tsx -t "运行状态轻量刷新失败"`、`npm test -- src/App.test.tsx -t "运行状态轻量刷新失败|运行状态会轻量动态刷新|较旧的会话快照晚返回|窗口操作会使用自身刷新结果|启动后会短延迟刷新会话|启动确认刷新失败不会清空已有运行状态|账号卡片运行状态来自浏览器会话快照|窗口操作前会刷新轻量会话状态|检查窗口|前置选中运行账号|平铺窗口|布局同步"`、`npm test`、`cargo test --manifest-path src-tauri/Cargo.toml`、`npm run build`、`npm run tauri:build`、`codesign --verify --deep --strict --verbose=2 src-tauri/target/release/bundle/macos/MultiChrome.app`、`git diff --check`：通过。当前前端测试 114 个，Rust 测试 38 个，已重新生成 macOS `.app`。Claude CLI 红队本轮 60 秒无输出，已中断，未取得有效审查结果。
- 浏览器会话编排 V0.9 使用 TDD：先添加“启动确认扫到停止时会短暂保留启动中状态”失败测试，确认旧行为会把“启动中”撤掉；随后加入 `STARTING_SESSION_GRACE_MS` 和 fresh starting 合并保护。阶段验证执行 `npm test -- src/App.test.tsx -t "启动确认扫到停止"`、`npm test -- src/App.test.tsx -t "启动命令成功后先标记为启动中|启动后会短延迟刷新会话|启动确认扫到停止|启动确认刷新失败不会清空已有运行状态|批量打开会标记启动中|项目打开会标记启动中|账号卡片运行状态来自浏览器会话快照|运行状态会轻量动态刷新|运行状态轻量刷新失败|较旧的会话快照晚返回|窗口操作会使用自身刷新结果"`：通过。Claude CLI 红队本轮 25 秒无输出，已中断，未取得有效审查结果。
- 浏览器会话编排 V1.0 使用 TDD：先新增 `src/browserSessions.test.ts`，确认缺少 `browserSessions` 模块时失败；随后新增 `src/browserSessions.ts`，测试 fresh starting 保留但不计入 running、running 快照立即覆盖 starting。再新增 `src/useBrowserSessions.ts` 并把 `App.tsx` 中会话状态、请求序号、启动确认 timer 和 starting 标记接入 hook。阶段验证执行 `npm test -- src/browserSessions.test.ts`、`npm test -- src/browserSessions.test.ts src/App.test.tsx -t "启动确认扫到停止|启动后会短延迟刷新会话|较旧的会话快照晚返回|窗口操作会使用自身刷新结果"`、`npm test`、`npm run build`：通过。当前前端测试 117 个。Claude CLI 红队本轮 30 秒无输出，已中断，未取得有效审查结果。
- 浏览器会话编排 V2.1/V2.2 使用 TDD：V2.1 先新增 `BrowserLaunchResult`，让启动失败不进入 transient `starting`；V2.2 先新增启动结果汇总失败测试，再让批量打开和项目打开显示失败账号和原因。Claude CLI 红队指出“项目多网址任一失败时账号整体失败”的语义需要测试固定，已补“项目账号任一网址失败时该账号整体不算打开成功”。阶段验证执行 `npm test -- src/browserSessionLaunch.test.ts src/App.test.tsx -t "汇总启动结果|批量打开部分失败|项目打开部分失败|项目账号任一网址失败"`、`npm test`、`npm run build`、`cargo test --manifest-path src-tauri/Cargo.toml`、`git diff --check`：通过。当前前端测试 125 个，Rust 测试 38 个。
- 浏览器会话编排 V2.3 使用 TDD：先添加最近启动事件纯函数测试和账号页“更多操作”启动记录 UI 失败测试，再实现内存态事件列表；账号卡、账号平台、批量打开和项目打开都会写入事件，UI 只展示最近 6 条，底层最多保留 30 条。Claude CLI 红队提示事件持久化和完整历史可后续做，当前按设计暂不持久化。阶段验证执行 `npm test -- src/browserSessionLaunch.test.ts`、`npm test -- src/App.test.tsx -t "最近启动|批量打开结果"`、`npm test`、`npm run build`、`cargo test --manifest-path src-tauri/Cargo.toml`：通过。当前前端测试 129 个，Rust 测试 38 个。
- 浏览器会话编排 V2.4 使用 TDD：先添加“持久化启动事件加载时排序/裁剪”“启动页加载持久化最近启动记录”“新增启动记录写入持久化记录”和 Rust `launch-events.json` 读写测试，并确认缺少接口时失败；随后实现 TS 规范化、前端 API、App 读写接线和 Rust 旁路 JSON 存储。Claude CLI 红队指出写入原子性和连续启动并发保存风险；已改为前端串行保存队列，并把 Rust 写入改为临时文件加 rename。阶段验证执行 `npm test -- src/browserSessionLaunch.test.ts src/App.test.tsx -t "持久化|启动页会加载|新增启动记录"`、`npm test -- src/App.test.tsx -t "连续启动账号时最近启动记录会按顺序串行保存|新增启动记录会写入|启动页会加载"`、`cargo test --manifest-path src-tauri/Cargo.toml save_and_load_browser_launch_events_round_trips_and_limits`：通过。
- 浏览器会话编排 V2.5 使用 TDD：先补 `launch-events.json` 损坏、混入坏 item 和原子写临时文件名去重测试，再实现 sidecar 读取容错和 temp 文件名递增计数。Claude CLI 红队指出同毫秒并发写入可能撞同一个 temp 文件，判断为真实风险并已修复；其它“坏 item 静默跳过”“重复 normalize”等为当前 sidecar 日志设计可接受。阶段验证执行 `npm test`、`cargo test --manifest-path src-tauri/Cargo.toml`、`npm run build`、`git diff --check`：通过。当前前端测试 133 个，Rust 测试 42 个。
- 浏览器会话编排 V2.6 使用 TDD：先添加队列摘要和批量/项目结束文案的纯函数失败测试，再新增 `BrowserLaunchQueueSummary`、`summarizeBrowserLaunchQueue`、`formatBulkLaunchQueueMessage` 和 `formatProjectLaunchQueueMessage`，并让批量打开、项目打开统一消费该模型。Claude CLI 红队指出批量打开保存分支仍看 `openedIds.size`，判断为真实的双来源小风险，已改为 `launchSummary.successCount > 0`；后续只读复审卡住无结论，已中断。阶段验证执行 `npm test -- src/browserSessionLaunch.test.ts`、`npm test -- src/App.test.tsx -t "批量打开|项目会按绑定账号|项目可以|项目打开|打开项目"`、`npm test`、`cargo test --manifest-path src-tauri/Cargo.toml`、`npm run build`、`git diff --check`：通过。当前前端测试 136 个，Rust 测试 42 个。
- 浏览器会话编排 V2.7 使用 TDD：原计划抽运行时进度模型，Claude CLI 红队判断与现有 queue summary 重叠、会增加循环复杂度；采纳该意见后缩小为“失败启动结果生成重试候选”纯模型。先添加“按原队列顺序生成重试账号”和“同一账号后续成功后不再重试”失败测试，再实现 `selectRetryableBrowserLaunchProfileIds`。阶段验证执行 `npm test -- src/browserSessionLaunch.test.ts`：通过。当前前端测试 138 个。
- 浏览器会话编排 V2.8 使用 TDD：先添加“批量打开失败后可以只重试失败账号”失败测试，确认 UI 没有重试入口；随后新增 `lastBulkLaunchRetry` 状态、复用批量打开内部函数，并在 `BulkActionBar` 中显示“重试失败 N”按钮。Claude CLI 本轮 60 秒无输出，已中断，未取得有效审查结果。阶段验证执行 `npm test -- src/App.test.tsx -t "批量打开失败后可以只重试失败账号"`、`npm test -- src/App.test.tsx -t "批量打开|网址库可以用选中账号直接打开网址|批量打开网址后会记录最近网址"`：通过。当前前端测试 139 个。
- 浏览器会话编排 V2.9 使用 TDD：先添加“新一轮批量打开开始时会清空旧重试入口”失败测试，确认旧按钮在第二轮启动中仍会显示；随后在 `openUrlForProfiles` 真正开始队列前清空 `lastBulkLaunchRetry`。Claude CLI 本轮 60 秒无输出，已中断，未取得有效审查结果。阶段验证执行 `npm test -- src/App.test.tsx -t "新一轮批量打开开始时会清空旧重试入口"`、`npm test -- src/App.test.tsx -t "重试失败|批量打开"`：通过。当前前端测试 140 个。
- 浏览器会话编排 V2.10 使用 TDD：先添加“修改批量网址会清空旧重试入口”失败测试，确认旧“重试失败 N”会在输入框改动后残留；随后新增 `updateBulkUrl`，让手动输入、常用/最近网址和网址库填入都清空旧重试入口。Claude CLI 红队指出 tooltip 只是告知型补丁，真正风险是输入框和旧失败队列状态分裂；该意见采纳。阶段验证执行 `npm test -- --run src/App.test.tsx -t "修改批量网址会清空旧重试入口"`：通过。
- 浏览器会话编排 V3.0 使用 TDD：先添加 `src/browserOperations.test.ts`，确认缺少 operation 模型时测试失败；随后新增 `BrowserOperation`、创建/启动/完成/取消函数，以及 `browserOperationStatusFromLaunchQueue`。Claude CLI 第一次输出伪工具调用，第二次红队有效指出 Operation 不应复制 `BrowserLaunchResult`、纯模型不要在真空里过度设计；已采纳为组合现有 `BrowserLaunchQueueSummary` 且暂不做 UI 大改。阶段验证执行 `npm test -- --run src/browserOperations.test.ts`：通过。
- 浏览器会话编排 V3 到 V4 计划已记录：V3 先把用户触发的浏览器操作纳入统一 operation 生命周期；V4 再把窗口对象、布局和同步从“命令式按钮”升级为可被编排器管理的资源。
- 浏览器会话编排 V3.1 使用 TDD：先添加“批量打开完成后会登记最近操作”和“停止批量打开后会把最近操作标记为已取消”失败测试，确认现有 UI 找不到 `最近操作记录`；随后把 `openUrlForProfiles` 接入 `BrowserOperation` 生命周期，并在“更多操作”增加轻量 `OperationList`。Claude CLI 红队有效指出取消语义和重试关系需要明确；本轮采纳为“停止则 operation 标记 cancelled，但 summary 仍显示已打开数量”，重试作为后续新 operation，不在 V3.1 做父子关联。
- 浏览器会话编排 V3.2 使用 TDD：先添加“项目打开完成后会登记最近操作”和“停止项目打开后会把最近操作标记为已取消”失败测试，确认旧实现没有项目 operation；随后把 `openProject` 接入 `BrowserOperation` 生命周期，并让项目 target 在最近操作里显示项目名。Claude CLI 本轮两次预审均无有效输出：第一次输出伪工具调用，第二次 30 秒无输出后中断，未采纳审查结论。阶段验证已执行 `npm test -- --run src/App.test.tsx -t "项目打开完成后会登记最近操作|停止项目打开后会把最近操作标记为已取消"` 和 `npm test -- --run src/App.test.tsx -t "项目|最近操作"`：通过。
- 浏览器会话编排 V3.3 使用 TDD：先添加“检查窗口完成后会登记窗口操作”和“同步布局失败后会登记窗口操作失败”失败测试，确认旧实现没有 `window-action` operation；随后新增窗口操作 helper，并接入检查、前置、平铺和同步布局。阶段验证已执行 `npm test -- --run src/App.test.tsx -t "检查窗口完成后会登记窗口操作|同步布局失败后会登记窗口操作失败"` 和 `npm test`：通过。
- 浏览器会话编排 V3.4 使用 TDD：先添加“批量打开进行中会单独显示当前操作”失败测试，确认旧 UI 找不到 `当前操作记录`；随后把 OperationList 拆成当前/最近两个 section，并把父组件传入改为完整 operation 列表。Claude CLI 本轮用 `--bare --permission-mode bypassPermissions --tools ''` 有效输出红队意见：内存增长已由 `MAX_BROWSER_OPERATIONS = 20` 覆盖；不持久化、并发冲突、取消语义和生命周期 race 都是 V3.5/V4 之前的已知边界。阶段验证已执行 `npm test -- --run src/App.test.tsx -t "批量打开进行中会单独显示当前操作|批量打开完成后会登记最近操作|停止批量打开后会把最近操作标记为已取消|项目打开完成后会登记最近操作|停止项目打开后会把最近操作标记为已取消|检查窗口完成后会登记窗口操作|同步布局失败后会登记窗口操作失败"`：通过。
- 浏览器会话编排 V3.5 使用 TDD：先添加 `findActiveBrowserOperationProfileConflicts` 纯模型失败测试，再添加“项目打开会避开正在执行批量打开的同账号”“批量打开会避开正在执行项目的同账号”“批量打开会避开正在单独启动的同账号”等 UI 失败测试；随后新增 active operation 冲突守卫，覆盖批量打开、项目打开、窗口操作、单账号打开和账号平台打开。Claude CLI 预审指出 queued 误杀、卡死无逃逸、部分成功阻塞、轻重操作饥饿和 check-then-act 非原子等风险；V3.5 先采用保守阻止策略，不做完整队列、强制解锁或持久化。Claude CLI 复审继续指出底层命令卡死会导致账号长期被 operation 锁住；该风险已采纳，新增 `withBrowserOperationTimeout`，启动和窗口命令 120 秒无返回会失败并释放 operation/旧启动状态。阶段验证已执行 v3.5 相关模型/UI 测试：通过。
- 浏览器会话编排 V4.0 使用 TDD：先添加 `src/browserWindows.test.ts`，确认缺少 `browserWindows` 模块时失败；随后新增窗口注册表和 `grid` 布局计划纯模型，并把“平铺窗口”接入该模型。窗口扫描失败会进入注册表错误项，窗口缺失会进入跳过项，容量超限时布局计划不生成移动目标，避免一边计算失败一边移动窗口。阶段验证已执行 `npm test -- --run src/browserWindows.test.ts`、`npm test -- --run src/App.test.tsx -t "平铺窗口|同步布局"`、`npm test`、`npm run build`、`cargo test --manifest-path src-tauri/Cargo.toml`、`git diff --check`：通过。当前前端测试 165 个，Rust 测试 42 个。
- 浏览器会话编排 V4.0 实机补充验证：用户已开启辅助功能权限，MultiChrome 和两个 Chrome profile 已打开；通过 Computer Use 操作 MultiChrome，“检查窗口”读到 `账号1 1 个窗口（864x1017 @ 0,38）` 与 `账号 2 1 个窗口（864x1016 @ 864,38）`；点击“平铺窗口”后 UI 短暂显示“平铺中”，完成后显示“已平铺 2 个窗口”；再次检查窗口坐标仍为左右分屏。结论是 V4.0 窗口注册表 + `grid` 布局计划接入平铺流程在真实授权桌面环境可用。
- 浏览器会话编排 V4.1 已完成：同步布局接入 `browserWindows` 窗口注册表，PR #3 `https://github.com/1572817043/mutiChrome/pull/3`，commit `1e58ec7d536e29181d391b2b2a9c243832598128`。新增 `buildWindowLayoutSyncPlan` 纯模型，覆盖 source/target 的 `missing`、`minimized`、`window-error` 建模；source 不进入 `placements`，跳过/失败计数只统计目标账号；目标读取失败会跳过并继续同步其它目标。Claude 改代码前反证审查有效，采纳 `sourceWindowError`、最小化保护、目标失败不中断等约束；Claude 提交前 diff 红队审查有效，采纳补 App 层目标读取失败测试。阶段验证执行 `rtk npm test -- --run src/App.test.tsx -t "布局同步会跳过读取失败的目标并继续同步其它目标"`、`rtk npm test -- --run src/App.test.tsx -t "同步布局|布局同步"`、`rtk npm test`、`rtk npm run build`、`rtk cargo test --manifest-path src-tauri/Cargo.toml`、`rtk git diff --check`：全部通过。本轮未做 macOS 辅助功能实机验证，因为未改底层 AX/Rust 窗口 API、`list_profile_windows`、`set_profile_window_bounds`、窗口 bounds 写入和读回机制；真实窗口同步继续纳入后续使用观察。
- 浏览器会话编排 V4.3 已完成：标准化检查窗口、平铺窗口、同步布局的 operation summary，PR #4 `https://github.com/1572817043/mutiChrome/pull/4`，commit `ca17053feedf0abf3cd8cb1bdcdfb8811a1c2543`。新增 `WindowOperationSummary`、inspect/tile/sync-layout summary builder、formatter 和 type guard；`OperationList` 优先展示标准窗口 summary，旧 fallback 保留；`App.tsx` 仅替换 summary 写入，未改 toast 文案、operation status、窗口执行流程或 Rust；未做 V4.2/V4.4/V5。Claude 改代码前反证审查有效；提交前 diff 红队审查有效，采纳检查窗口失败 detail、容量超限 `skippedCount` 补算 `noWindowCount`、`isWindowOperationSummary` action/可选字段类型校验、reason/label 分支防御处理和 tile `unchangedCount` 测试。阶段验证执行 `rtk npm test -- --run src/browserOperations.test.ts`、`rtk npm test -- --run src/browser/operations/OperationList.test.tsx`、`rtk npm test -- --run src/App.test.tsx -t "检查窗口|平铺窗口|同步布局|布局同步"`、`rtk npm test`、`rtk npm run build`、`rtk cargo test --manifest-path src-tauri/Cargo.toml`、`rtk git diff --check`：全部通过。本轮未做 macOS 辅助功能实机验证，因为未改底层 AX/Rust 窗口 API、坐标换算、bounds 写入/读回或新增窗口入口；按窗口类 PR 验证规则，本轮实机验证可选。
- 阶段 1“建立无行为拆分基线”完成：只从 `src/App.tsx` 末尾抽出纯 helper 和显示/解析 helper，新增 `src/domain/projectModel.ts`、`src/domain/batchProfileParser.ts`、`src/domain/urlLibraryModel.ts`、`src/shared/urlHelpers.ts`、`src/shared/formatHelpers.ts`、`src/shared/screenWorkArea.ts`、`src/shared/windowAutomationErrors.ts`，并把 `cloneProfileForDraft` 收进 `src/domain/profileModel.ts`。`App.tsx` 从 6794 行降到 6342 行；本阶段不改业务状态和 UI 结构。先写新增 helper 测试并确认红灯，再移动实现。阶段验证执行 `npm test`、`npm run build`、`cargo test --manifest-path src-tauri/Cargo.toml`、`git diff --check`：通过。当前前端测试 177 个，Rust 测试 42 个。
- 阶段 2“拆 URL Library 与记录展示组件”完成：只从 `src/App.tsx` 搬出已经是独立函数组件的低耦合 UI，不做 UI 重设计、不新增功能、不改业务状态和用户可见行为。新增 `src/url-library/UrlLibraryView.tsx`、`src/url-library/UrlShortcutGroup.tsx`、`src/browser/operations/OperationList.tsx`、`src/browser/launch/LaunchEventList.tsx`，并新增对应组件边界测试 `src/url-library/UrlLibraryView.test.tsx`、`src/browser/operations/OperationList.test.tsx`、`src/browser/launch/LaunchEventList.test.tsx`。`App.tsx` 从 6342 行降到 5750 行；阶段 1 之后仍留在 `App.tsx` 末尾的 profile/import helper 本阶段不碰。阶段验证执行 `npm test -- --run src/url-library/UrlLibraryView.test.tsx src/browser/operations/OperationList.test.tsx src/browser/launch/LaunchEventList.test.tsx`、`npm test -- --run src/App.test.tsx -t "网址库|最近操作|当前操作|最近启动|批量打开"`、`npm test -- --run src/browserOperations.test.ts src/browserSessionLaunch.test.ts`、`npm test`、`npm run build`、`cargo test --manifest-path src-tauri/Cargo.toml`、`git diff --check`：通过。当前前端测试 182 个，Rust 测试 42 个。
- 阶段 3“抽浏览器操作编排 hook”完成：只从 `src/App.tsx` 抽出浏览器 operation 状态和胶水函数，新增 `src/browser/operations/useBrowserOperations.ts` 与 `src/browser/operations/useBrowserOperations.test.tsx`。`browserOperations` 状态、operation ID 生成、upsert/裁剪、profile open/window action/bulk open/project open 生命周期、active operation 冲突提示、启动和窗口命令 120 秒超时包装均迁入 hook；`App.tsx` 改为调用 hook API。最近启动事件保存队列仍保留在 `App.tsx`，避免本轮扩大 sidecar 持久化风险。`App.tsx` 从 5750 行降到 5589 行；本阶段不做 UI 重设计、不新增 operation 能力、不改变用户可见行为。阶段验证执行 `npm test -- --run src/browser/operations/useBrowserOperations.test.tsx` 先红灯、再绿灯；`npm test -- --run src/browserOperations.test.ts src/browser/operations/useBrowserOperations.test.tsx`、`npm test -- --run src/App.test.tsx -t "批量打开|重试失败|项目打开|检查窗口|平铺窗口|同步布局|前置窗口|正在执行|超时"`、`npm test`、`npm run build`、`cargo test --manifest-path src-tauri/Cargo.toml`、`git diff --check`：通过。当前前端测试 186 个，Rust 测试 42 个。
- 阶段 4“拆账号与批量栏 UI”完成：只从 `src/App.tsx` 迁出账号相关低状态/展示型组件和批量栏 UI，新增 `src/profiles/ProfileCard.tsx`、`src/profiles/EditProfileDialog.tsx`、`src/profiles/AccountPlatformEditor.tsx`、`src/profiles/AccentColorPicker.tsx`、`src/profiles/BatchCreateProfilesDialog.tsx`、`src/profiles/DeleteConfirmDialog.tsx`、`src/profiles/profileAppearance.ts`、`src/bulk-actions/BulkActionBar.tsx`、`src/bulk-actions/BatchDeleteConfirmDialog.tsx`，并新增组件边界测试 `src/profiles/ProfileComponents.test.tsx` 和 `src/bulk-actions/BulkActionBar.test.tsx`。账号 CRUD、批量打开、重试失败、停止队列、窗口操作、保存队列和最近启动事件持久化仍留在原业务编排层，没有顺手抽走。`App.tsx` 从 5589 行降到 4501 行；本阶段不做 UI 重设计、不新增功能、不改变交互文案。阶段验证执行 `npm test -- --run src/profiles/ProfileComponents.test.tsx src/bulk-actions/BulkActionBar.test.tsx` 先红灯、再绿灯；`npm test -- --run src/profiles/ProfileComponents.test.tsx src/bulk-actions/BulkActionBar.test.tsx src/App.test.tsx -t "账号卡片|保存账号|账号平台|批量新建|批量删除|删除账号|批量打开|重试失败|停止|检查窗口|平铺窗口|同步布局|前置窗口"`、`npm test`、`npm run build`、`cargo test --manifest-path src-tauri/Cargo.toml`、`git diff --check`：通过。当前前端测试 191 个，Rust 测试 42 个。
- 阶段 5“拆 Projects、Settings 与 Data Safety UI”完成：只从 `src/App.tsx` 迁出项目、设置和数据安全相关展示/弹窗组件，不做 UI 重设计、不新增备份或项目能力、不改变用户可见行为。新增 `src/projects/ProjectsView.tsx`、`src/projects/ProjectCard.tsx`、`src/projects/EditProjectDialog.tsx`、`src/projects/ProjectDeleteConfirmPanel.tsx`、`src/settings/SettingsDialog.tsx`、`src/data-safety/HealthReportPanel.tsx`、`src/data-safety/RepairResultPanel.tsx`、`src/data-safety/FullRestoreConfirmDialog.tsx`，并新增组件边界测试 `src/projects/ProjectComponents.test.tsx`、`src/settings/SettingsDialog.test.tsx`、`src/data-safety/DataSafetyPanels.test.tsx`。项目打开队列、停止队列、项目 CRUD、设置保存、健康检查、轻量备份、完整备份/恢复等业务编排仍留在 `App.tsx`。`App.tsx` 从 4501 行降到 3252 行。阶段验证执行 `npm test -- --run src/projects/ProjectComponents.test.tsx src/settings/SettingsDialog.test.tsx src/data-safety/DataSafetyPanels.test.tsx` 先红灯、再绿灯；`npm test -- --run src/App.test.tsx -t "项目|保存项目|批量导入网址|停止|设置|健康|备份|恢复|孤儿"`、`npm test`、`npm run build`、`cargo test --manifest-path src-tauri/Cargo.toml`、`git diff --check`：通过。当前前端测试 204 个，Rust 测试 42 个。
- 第二轮阶段 1“主工作流清晰度”完成：只打磨账号页最常用路径，不新增功能、不改底层业务流程、不改批量打开队列顺序、不改停止队列语义、不改重试失败候选逻辑、不改批量新建解析规则。`src/bulk-actions/BulkActionBar.tsx` 增加未选择账号、已选择账号、打开中、空网址新标签、指定网址和重试最近失败的轻量说明；批量打开主按钮按输入状态显示“打开新标签”或“打开指定网址”，“重试失败 N”调整为“重试最近失败 N”，并说明只重试最近一次批量打开失败账号；常用/最近网址说明为点击后只填入输入框。`src/profiles/BatchCreateProfilesDialog.tsx` 增加支持格式说明，明确支持“名称, 标签, 备注”、“名称 | 标签 | 备注”和表格 Tab 分隔粘贴；解析为空时显示“还没有解析到账号，请至少填写账号名称”。`src/styles.css` 只补充新增说明和空状态的轻量样式及深色模式适配；`src/App.test.tsx` 只更新按钮文案断言以匹配新语义。`src/browserSessionLaunch.ts` 和后端未做本轮修改。阶段验证先让 `src/bulk-actions/BulkActionBar.test.tsx`、`src/profiles/ProfileComponents.test.tsx` 红灯，再实现转绿；随后执行 `npm test -- --run src/bulk-actions/BulkActionBar.test.tsx src/profiles/ProfileComponents.test.tsx src/browserSessionLaunch.test.ts`、`npm test -- --run src/App.test.tsx -t "批量打开|重试失败|批量新建|最近网址|常用网址|新标签"`、`npm test`、`npm run build`、`git diff --check`、`cargo test --manifest-path src-tauri/Cargo.toml`：通过。当前前端测试 206 个，Rust 测试 42 个。
- 第二轮阶段 2“资料管理空状态与行操作”完成：只打磨项目页和网址库，不新增功能、不做 UI 大重设计、不改变产品定位、不改项目打开队列语义、不改项目停止语义、不改项目多网址打开语义、不改网址库 CRUD、回填批量网址或用选中账号打开网址逻辑。`src/projects/ProjectsView.tsx` 更新项目空列表和搜索无结果文案，让项目说明匹配“多个入口网址 + 绑定账号 + 打开间隔”的当前语义。`src/projects/ProjectCard.tsx` 增加未绑定账号、未设置网址、正在打开和被其他项目占用时的可见禁用原因；打开按钮会更明确地禁用并显示“不可打开”，编辑按钮仍只沿用原有 `disabled` 规则。`src/url-library/UrlLibraryView.tsx` 在保留 Notion 风格表格和“新建”行的前提下补充空库说明、搜索无结果说明；网址库行级“打开”在未选择账号时会显示“先选择账号”并禁用，有选中账号时仍调用原有直接打开回调。`src/styles.css` 只补充项目卡片提示、网址库空状态和无账号提示的局部样式及深色模式适配；`src/App.test.tsx` 只更新网址库空状态断言以匹配新语义。阶段验证先让 `src/projects/ProjectComponents.test.tsx` 和 `src/url-library/UrlLibraryView.test.tsx` 红灯，再实现转绿；随后执行 `npm test -- --run src/projects/ProjectComponents.test.tsx src/url-library/UrlLibraryView.test.tsx`、`npm test -- --run src/App.test.tsx -t "项目|网址库"`、`npm test`、`npm run build`、`cargo test --manifest-path src-tauri/Cargo.toml`：通过。当前前端测试 209 个，Rust 测试 42 个。
- 第二轮阶段 3“设置与数据安全稳定感”完成：只打磨设置弹窗里的目录健康、轻量备份、完整备份/恢复和完整恢复危险确认，不新增功能、不做 UI 大重设计、不改变产品定位、不改健康检查、备份或恢复调用时机，不改数据结构，不碰 Rust 后端。`src/settings/SettingsDialog.tsx` 增加根目录和 Chrome 路径检测状态说明，目录健康区说明“先检查、再修复低风险项或手动登记孤儿目录”，轻量备份明确只保存 `profiles.json` 里的账号索引、项目、网址库和设置，不包含 `profiles/` 下的 Chrome profile 文件夹；完整备份/恢复区补充“预览本次范围、创建完整备份、扫描恢复目录、确认恢复”的步骤提示。`src/data-safety/HealthReportPanel.tsx` 增加检查通过、有提醒/错误的状态说明，并明确孤儿 Profile 登记只加入账号索引，不复制、不移动、不删除原目录。`src/data-safety/RepairResultPanel.tsx` 增加自动修复动作数量和低风险修复范围说明。`src/data-safety/FullRestoreConfirmDialog.tsx` 在只读核对当前恢复实现后，补充新增/覆盖数量、覆盖同 ID 账号资料和 Chrome profile 文件夹、保留备份外其他账号、不会合并同 ID profile 文件夹内容的确认提示。`src/styles.css` 只补充新说明、步骤提示和危险说明的局部样式及深色模式适配。阶段验证先让 `src/settings/SettingsDialog.test.tsx` 和 `src/data-safety/DataSafetyPanels.test.tsx` 红灯，再实现转绿；随后执行 `npm test -- --run src/settings/SettingsDialog.test.tsx src/data-safety/DataSafetyPanels.test.tsx`、`npm test -- --run src/App.test.tsx -t "设置|健康|备份|恢复|孤儿"`、`npm test`、`npm run build`、`cargo test --manifest-path src-tauri/Cargo.toml`：通过。当前前端测试 209 个，Rust 测试 42 个。
- 第二轮阶段 4“操作记录和窗口提示轻量收口”完成：只打磨更多操作里的 `OperationList` 和 `LaunchEventList` 展示，不新增功能、不做 UI 大重设计、不改变产品定位；未改 operation 生命周期、排序、裁剪和内存策略；未改最近启动事件生成、保存、读取逻辑；未碰 `App.tsx` 业务流程、Rust 后端或窗口 API 调用顺序。`src/browser/operations/OperationList.tsx` 把当前/最近操作行改为更清晰地展示状态、操作类型、目标、账号数量和进度；批量/项目启动失败只展示前 1-2 条失败原因并提示另有 N 个失败；取消操作显示已有取消原因；窗口操作只展示 summary 里已有的数量字段，不做推断或运行时转换。`src/browser/launch/LaunchEventList.tsx` 给最近启动记录补充账号名、来源、目标和失败原因标签，保留成功/失败状态、来源、URL 和失败 message。`src/styles.css` 只补充这两个列表的局部布局和深色模式适配；组件测试覆盖失败原因截断、取消原因、窗口 summary 已有字段和启动记录标签。阶段验证先让 `src/browser/operations/OperationList.test.tsx`、`src/browser/launch/LaunchEventList.test.tsx` 红灯，再实现转绿；随后执行 `npm test -- --run src/browser/operations/OperationList.test.tsx src/browser/launch/LaunchEventList.test.tsx`、`npm test -- --run src/App.test.tsx -t "最近操作|当前操作|最近启动"`、`npm test`、`npm run build`、`cargo test --manifest-path src-tauri/Cargo.toml`：通过。当前前端测试 211 个，Rust 测试 42 个。

## 已知限制

- 通过 Codex 内置浏览器完成本地页面截图验收；窗口平铺已由用户在真实 macOS `.app` 里验证，重复点击已运行账号的“必定新增空白标签”仍需继续观察 Chrome 自身行为。
- 外部启动器当前是 shell `.app` 包装器：能作为不同图标的启动入口，但 Chrome 窗口实际进程仍属于 `/Applications/Google Chrome.app`，所以运行中的 Dock 图标可能仍显示 Chrome 自身图标。
- stay-open AppleScript helper 已能保持独立账号入口进程，并能接收 reopen 事件；视觉层面的 Dock 点击聚焦效果仍需要用户肉眼确认。若系统未授予辅助功能权限，`System Events` 聚焦可能失败，但脚本不会因此重复启动 Chrome。
- 代理、群控、指纹隔离、任务系统、密码安全存储、网页自动填入账号密码均未进入 MVP。
- 软件内 AI 助手、实时窗口同步、群控均未进入当前 MVP。
- 网址库暂不做 favicon、自动标题抓取、AI 扫描导入、账号绑定规则或网页自动填表；当前只是本地常用网址资料库和批量打开入口。
- 窗口管理仍是进程级/手动操作能力：运行状态依赖 macOS `ps` 进程参数；“切换到账号”“检查窗口”“前置窗口”“平铺窗口”“同步布局”依赖 macOS Accessibility API，仍需要系统辅助功能权限。现在能读取并设置首个窗口位置/尺寸，窗口检查能显示首个窗口是否已最小化；平铺和同步布局都已接入窗口注册表，平铺会跳过无可读窗口账号、尊重屏幕可用区域偏移，窗口过多时提示分批处理，多窗口账号会提示仅平铺首个窗口；布局同步只复制主账号首窗的位置和大小到其它运行账号首窗，并会跳过最小化或读取失败目标；平铺和同步都已改为设置后读回坐标，未接近目标 bounds 会提示“未生效”，成功后会尝试批量前置相关窗口；权限类失败会提示辅助功能设置位置，但仍不能识别具体标签页，也不是鼠标键盘实时同步。
- 完整 profile 备份当前是整目录复制，不做增量、压缩、加密或云同步；大 profile 目录备份会占用较多磁盘空间。
- 批量删除文件夹不是事务型操作：如果删除多个 profile 文件夹时中途失败，已经删除成功的文件夹不会自动还原；账号索引会保留，后续可通过健康检查发现缺失目录。

## 稳定观察

- 稳定观察期起点：2026-07-23，`main` 与 `origin/main` 一致，工作区干净，从功能开发转入稳定观察；本阶段不新增功能、不大重构、不重写 UI。
- 当前基线 commit：`57e48b855d16c11a74bdafec1f7464a7f52d392b`。
- 遗留项位置：`stash@{0}`，内容为遗留图标和旧状态报告隔离包；stash message：`On codex/stage-4-profile-bulk-ui: protect legacy icons and project status report before main sync`；stash hash：`f91e1b59729b687f4686fd55d7bedd10dfd9a261`；观察期内不恢复 stash、不处理图标。
- 观察清单：日常启动 MultiChrome；观察账号卡片启动、批量打开、项目打开、运行状态刷新、窗口检查/前置/平铺/同步布局、设置里的健康检查和备份恢复入口是否出现真实异常；重点记录可复现步骤、期望结果、实际结果、是否影响数据安全。
- 后续处理规则：只处理真实复现问题；每个问题单独开小分支；每个修复保持小 PR；默认不顺手加功能、不扩大重构、不借 bug 修复重写 UI。

## 下一步

- 下一步优先做 V4.2：轻量布局预设模型，先只做纯模型和现有按钮可消费的最小接线，不做复杂预设管理 UI。
- V4.4 再考虑轻量窗口面板，只显示选中账号窗口注册表状态，不做实时控制台。
- V5 从同步控制器基础开始：先做 `sync-bounds`、dry-run 预览和结果详情；鼠标、键盘、滚动、标签页这类交互同步只作为技术预研，不能直接进入 MVP 主流程。
- 目录大小/容量占用如果后续确实需要展示，优先做左侧独立统计模块或设置内统计，不回到账号卡片常驻展示。
- 密码存储需要先设计本地安全方案，不直接明文写入 `profiles.json`；网页自动填入账号密码暂不做。
- Dock 图标区分暂时不做，除非后续重新明确需要。
- AI 助手作为后期路线记录，等基础功能经过实际使用和小 bug 修复后再进入设计。
